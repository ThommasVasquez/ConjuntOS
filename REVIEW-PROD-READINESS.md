# EN-CONJUNTO — Production-Readiness Review

_Multi-agent adversarially-verified review (14 finders → skeptic verification → kept 52 of 116 candidates; 64 refuted/dropped). Generated 2026-06-11._

---

## ✅ Fixes applied (2026-06-11)

Verified after every change: `cargo fmt`/`clippy -D warnings` clean, **65 backend tests pass**, frontend `tsc` clean, `next build` succeeds.

### Blockers fixed
1. **CRITICAL — migrate decimal=0 corruption (#1):** added `rust_decimal` (`db-tokio-postgres`) and decode NUMERIC via `Decimal` in `migrate/src/steps.rs` `dec_str`/`dec_str_opt`. Money + coeficiente + vote weights now migrate exactly instead of silently zeroing.
2. **Clasificados unusable (#5/#6):** frontend category IDs aligned to the backend `CatLocal` enum (`RESTAURANTE…OTRO`); fixed the `titulo`→`nombre` field bug. Publishing + filtering now work.
3. **vehiculos.placa cross-tenant (#4/#7):** new migration drops the global UNIQUE, adds `UNIQUE (conjunto_id, placa)`.
4. **Asamblea poderes IDOR / cross-tenant (#2/#3/#10):** `create_poder` now requires caller == otorgante (or admin) and validates both parties belong to the conjunto; `compute_effective_coeficiente` + `get_quorum` scope every usuario/unidad lookup by `conjunto_id`.
5. **Poder double-count + unit-less ballot stuffing (#8/#9):** `cast_voto` rejects unit-less voters, removes the fabricated 0.01 weight, and blocks an otorgante from voting when a verified poder represents them.
6. **Storage fake fallback (#11):** removed `FakeStorage`; production uses MinIO/S3 only, and `UnconfiguredStorage` fails uploads loudly (Law 4) instead of persisting fake `localhost` URLs.
7. **No request timeout (#12):** added `TimeoutLayer` (30s → 408).
8. **Reservation double-booking (#15):** GiST `EXCLUDE` exclusion constraint on `reservas`; exclusion violations mapped to 409.
9. **JWT not revocable (#13):** added `usuarios.password_changed_at`; `extract.rs` rejects any token issued before it; `change_password` bumps it → instant session invalidation.
10. **localStorage 30-day token (#14/#19):** token is now in-memory only (httpOnly cookie is the reload-surviving credential); WebSocket auth uses a new short-lived `GET /auth/ws-ticket` (120s) instead of a persisted JWT.
11. **--seed-demo prod guard (#17):** refuses to seed unless `ENCONJUNTO_ALLOW_SEED=1`.

### Hardening added (beyond blockers)
- Graceful shutdown on SIGTERM/Ctrl-C; `CatchPanicLayer` (panic → 500, worker survives); WS reconnect backoff + stop-on-401.
- `admin-finanzas` page wired to real `/admin/stats` data (was a hardcoded stub); stock-photo fallbacks → local `/placeholder.svg`; deleted dead `src/lib/seed-utils.ts`.

### Deferred (documented, not fixed)
- **Push notifications (#service):** no real VAPID web-push implementation exists (deps were removed in M9). Needs to be built — not faked. Currently log-only.
- **Migrate transactions (#16) & richer `--verify` FK/field checks (#18):** robustness for the one-time migrate tool.
- **Rate limiting on login/PIN (#medium), Secure cookie in same-site mode, length bounds on free-text fields** — see findings below.

---

## Build/test ground truth

| Check | Result |
|---|---|
| `cargo fmt --all --check` | PASS |
| `cargo clippy --all-targets -- -D warnings` | PASS (0 warnings) |
| `cargo test --workspace` | PASS (65 tests) |
| frontend `tsc --noEmit` | PASS (0 errors) |
| `next build` | (separate; note `ignoreBuildErrors:true`) |

> Note: asamblea (M6) and AI (M7) domains have **no integration tests**. Frontend page-level findings were partly lost when the model rate limit killed their verifiers mid-run; this report is backend-heavy + frontend auth/client.

## Counts
Critical **1** · High **18** · Medium **12** · Low **20** · Info **1** · Launch-blockers **12**


## Critical (1)

### CRITICAL-1 **[BLOCKER]** — All Decimal/NUMERIC columns silently migrate as 0 — tokio-postgres has no decimal feature enabled
- **Where:** `backend/migrate/src/steps.rs:82-93`  · _data-integrity / migrate-binary_ · confidence: high
- **Why:** backend/migrate/Cargo.toml enables tokio-postgres features `with-uuid-1, with-chrono-0_4, with-serde_json-1` but NO decimal feature (no rust_decimal / no bigdecimal). The legacy columns coeficiente, monto, precio, total, depositoMonto, area are all Prisma `Decimal` → Postgres NUMERIC (confirmed in prisma/schema.prisma lines 336,351,424,440,470,483,515,558,563,678). tokio-postgres provides NO `FromSql` impl that decodes NUMERIC into `String` and its `f64` impl only accepts FLOAT4/FLOAT8 — so BOTH `try_get::<String>` and `try_get::<f64>` return WrongType errors and `dec_str` falls through to the literal "0" (and `dec_str_opt` returns None). Result: every monetary amount (pagos/gastos/recibos/pedidos/productos/inmuebles), every ownership coefficient (unidades.coeficiente), and every assembly vote weight (asamblea_votos.coeficiente) is written as 0/NULL. This silently corrupts financials and legally-binding assembly vote weighting. The API backend by contrast uses `bigdecimal` for these columns (backend/Cargo.toml:12, backend/api/Cargo.toml:21), confirming the migrate crate is the outlier that cannot read NUMERIC.
- **Fix:** Add a decimal-capable FromSql to the migrate crate: enable `tokio-postgres` feature `with-rust_decimal-1` (or read via `SELECT ..., monto::text` casts so the value arrives as text). Then decode into a real Decimal/text and pass it through. Add a verify spot-check that compares a sampled monetary value old-vs-new so a 0-fill regression is caught.
- **Verifier:** The quoted code at backend/migrate/src/steps.rs:82-93 is exact and unchanged. Verified the full chain: (1) backend/migrate/Cargo.toml:21 enables tokio-postgres features with-uuid-1, with-chrono-0_4, with-serde_json-1 only — no decimal feature. (2) Decisive proof in backend/Cargo.lock: resolved postgres-types 0.2.13 lists only chrono, serde_json, uuid as type deps — NO bigdecimal/rust_decimal/bit-vec/eui48, so no FromSql impl for NUMERIC is compiled in. (3) All legacy reads are `SELECT * FROM "Table"` with NO ::text/to_char cast on decimal columns; the CAST($n AS NUMERIC) appears only on the IN


## High (18)

### HIGH-1 **[BLOCKER]** — Cross-tenant / IDOR poder creation: otorgante_id and apoderado_id are trusted from the request body and never verified against the caller's conjunto
- **Where:** `backend/api/src/domains/asamblea/handlers.rs:591-613`  · _tenancy / auth-tenancy_ · confidence: high
- **Why:** create_poder only calls verify_asamblea_tenant on the {id} path param (the assembly), then inserts a poder row using otorgante_id and apoderado_id taken verbatim from the JSON body. Neither id is checked to belong to user.conjunto_id (no usuario_en_conjunto / scoped lookup). Any authenticated resident can POST /asambleas/{id}/poderes with an arbitrary otorgante_id - including a user UUID from another conjunto. The route is NOT admin-gated (no guard::require), so any resident can create poderes. This becomes a real voting-weight exploit via finding #2: once a poder is marked verificado, compute_effective_coeficiente adds the otorgante's unit coefficient to the apoderado's vote, with no tenant boundary on the otorgante. This is both an IDOR (assigning a power-of-attorney record referencing users the caller has no relationship to) and a Law-2 cross-tenant data-integrity leak.
- **Fix:** In create_poder, validate that both otorgante_id and apoderado_id resolve to active users in user.conjunto_id before insert (reuse a usuario_en_conjunto-style existence check for each), and reject otherwise with 404/400. Consider also requiring the caller to be the otorgante or an admin, since residents should not be able to fabricate powers-of-attorney on behalf of arbitrary owners.
- **Verifier:** The quoted code at backend/api/src/domains/asamblea/handlers.rs:591-628 is real and unchanged. create_poder() calls only repo::verify_asamblea_tenant(conn, asamblea_id, user.conjunto_id) (repo.rs:25-37), which validates ONLY that the assembly belongs to the caller's conjunto — it never touches otorgante_id or apoderado_id. The repo insert (repo.rs:505-512) is a raw diesel::insert_into with no validation. The DTO (dto.rs:323-326) takes otorgante_id and apoderado_id as plain Uuids straight from the JSON body. No scoped lookup / usuario_en_conjunto / conjunto_id filter is applied to either id any

### HIGH-2 **[BLOCKER]** — compute_effective_coeficiente aggregates otorgante unit coefficients with no conjunto scoping, enabling cross-tenant vote-weight inflation
- **Where:** `backend/api/src/domains/asamblea/repo.rs:265-296`  · _tenancy / auth-tenancy_ · confidence: high
- **Why:** When casting a vote, the effective coefficient is base (own unit) + sum of the coefficients of every otorgante for which the caller is a verified apoderado. The otorgante->usuario->unidad->coeficiente chain is filtered only by id, never by conjunto_id. So if a poder row references an otorgante in another conjunto (made possible by finding #1) and is verified, that other tenant's unit coefficient is summed into this caller's vote weight, corrupting quorum/voting results across tenant boundaries. Even within a single tenant this lets an attacker borrow another owner's full coefficient if they can get a forged poder verified.
- **Fix:** Scope the otorgante and unit lookups by the asamblea's conjunto_id (join through asambleas, or pass conjunto_id and filter usuarios.conjunto_id / unidades.conjunto_id). Fixing finding #1 is necessary but not sufficient; defense-in-depth here ensures a bad poder row can never contribute foreign-tenant coefficient.
- **Verifier:** The quoted code at backend/api/src/domains/asamblea/repo.rs:265-296 is real and unchanged. compute_effective_coeficiente sums otorgante unit coefficients via asamblea_poderes -> usuarios -> unidades, filtering ONLY by id/unidad_id and never by conjunto_id, even though the schema (api/src/db/schema.rs) shows both usuarios and unidades carry a conjunto_id column. The reviewer's claimed missing filter is genuinely absent. Confirming the pattern is wrong: the sibling function get_quorum (repo.rs:336-338) DOES scope unidades by conjunto_id, so the correct pattern exists in the same file and was sim

### HIGH-3 **[BLOCKER]** — Global UNIQUE on vehiculos.placa leaks/blocks across tenants (Law 2 boundary)
- **Where:** `backend/migrations/2026-06-10-000004_parqueadero/up.sql:4-13`  · _tenancy / domains-operational_ · confidence: high
- **Why:** The placa unique constraint is global, not scoped to conjunto_id. crear_vehiculo (backend/api/src/domains/parqueadero/repo.rs:50-57) relies on this unique violation to return 409. Consequences: (1) a resident of conjunto B cannot register a plate that already exists in conjunto A, even though the two communities are unrelated tenants, a cross-tenant functional break; (2) it is a cross-tenant oracle: by observing the 409 vs success, a user in one conjunto can probe whether a given plate is registered anywhere in the entire system. Same plate legitimately recurs across different gated communities, so this will misfire in production.
- **Fix:** Replace the global unique with a per-tenant one: drop the column-level UNIQUE and add `CONSTRAINT vehiculos_placa_conjunto_uk UNIQUE (conjunto_id, placa)`. Keep the unique-violation -> 409 mapping; it will then correctly mean 'already registered in this conjunto'.
- **Verifier:** The quoted code is real and unchanged. backend/migrations/2026-06-10-000004_parqueadero/up.sql line 8 declares `placa text NOT NULL UNIQUE` inside CREATE TABLE vehiculos — a table-wide (global) unique constraint, NOT `UNIQUE (conjunto_id, placa)`. It is the only place the table is created; grep shows no later migration adds/alters the constraint, and the only other `placa` columns (registros_parqueadero line 61, vigilancia line 14) are plain `text` with no uniqueness. So the only uniqueness on a plate is global. crear_vehiculo (backend/api/src/domains/parqueadero/repo.rs:50-57) does a bare `i

### HIGH-4 **[BLOCKER]** — Clasificados create + filter is fully broken: frontend categories never match backend CatLocal enum
- **Where:** `src/app/(app)/clasificados/page.tsx:34-39, 293, 304`  · _contract / domains-content_ · confidence: high
- **Why:** The backend CatLocal enum only accepts RESTAURANTE, TIENDA, LAVANDERIA, FARMACIA, OTRO (backend/api/src/db/enums.rs lines 165-170, enforced by the locales_categoria_check DB constraint). The frontend's posting form and category filter use a completely different vocabulary (GASTRONOMIA, CUIDADO, SERVICIOS, MASCOTAS, TECNOLOGIA). Because CreateClasificadoRequest.categoria is a required CatLocal, every POST /clasificados from this UI fails serde deserialization (HTTP 400/422), so no resident can ever publish a classified. The category filter `item.categoria === selectedCat` also never matches because stored values use the backend vocabulary. This endpoint is unusable from the shipped UI.
- **Fix:** Pick one canonical category vocabulary and align both sides. Either expand CatLocal (and the locales_categoria_check constraint + a migration) to the frontend's set, or change the frontend CATEGORIES ids to RESTAURANTE/TIENDA/LAVANDERIA/FARMACIA/OTRO. Add a contract test that posts each UI category value and asserts 200.
- **Verifier:** Verified across all four layers (file is at /Users/nkt/dev/EN-CONJUNTO/src/app/(app)/clasificados/page.tsx, not under backend/). 1) Frontend (page.tsx): CATEGORIES at lines 33-40 use GASTRONOMIA, CUIDADO, SERVICIOS, MASCOTAS, TECNOLOGIA (plus TODOS). The posting form defaults categoria:"GASTRONOMIA" (line 293) and the <select> (lines 349-357) only offers these same IDs. handleSubmit does api.post('/clasificados', form) (line 304) with the raw form. 2) api client (/Users/nkt/dev/EN-CONJUNTO/src/lib/api/client.ts): post does JSON.stringify(body) with NO key/value transformation. Field names (n

### HIGH-5 **[BLOCKER]** — Clasificados form writes title into a non-existent field, so nombre is always empty
- **Where:** `src/app/(app)/clasificados/page.tsx:289-299, 331`  · _correctness / domains-content_ · confidence: high
- **Why:** The title input's onChange writes to form.titulo, a key that does not exist in the form state (the backend field is nombre). form.nombre therefore stays empty, so the client-side guard `if (!form.nombre ...)` blocks every submit. Even if it didn't, the backend rejects empty nombre with BadRequest ('el nombre es obligatorio', clasificados/handlers.rs line 56-58). Combined with the category mismatch above, classified creation is doubly broken.
- **Fix:** Change the onChange to setForm({...form, nombre: e.target.value}). Add a test that types a title and submits.
- **Verifier:** The cited code is real and unchanged (file lives at repo root /Users/nkt/dev/EN-CONJUNTO/src/app/(app)/clasificados/page.tsx, not under backend/). Verified each link in the chain: 1) page.tsx:289-295 — useState init has keys nombre/descripcion/precio/categoria/whatsapp, NO titulo key. 2) page.tsx:327-333 — the title input binds value={form.nombre} but onChange writes setForm({...form, titulo: e.target.value}) (line 331). Writes go to a phantom 'titulo' key; form.nombre is never updated. Because the displayed value is bound to form.nombre (which stays ""), the field even appears to ignore type

### HIGH-6 **[BLOCKER]** — vehiculos.placa UNIQUE is global, not per-conjunto: cross-tenant collision and existence leak on trámite approval
- **Where:** `backend/migrations/2026-06-10-000004_parqueadero/up.sql:8`  · _tenancy / domains-content_ · confidence: high
- **Why:** The license-plate uniqueness constraint is global across all conjuntos. When an admin approves a VEHICULO trámite, tramites/repo.rs inserts into vehiculos with placa = v.placa.trim().to_uppercase(); a duplicate triggers a UniqueViolation that error.rs maps to 409 Conflict. If the same plate was already registered in a DIFFERENT conjunto, conjunto B's admin cannot approve the trámite (a legitimate, very common case: same plate format across cities, or a resident who moved). It also leaks the fact that the plate exists in some other tenant (a Law 2 isolation smell). Every other tenant table is scoped by conjunto_id; this one is not.
- **Fix:** Replace the global UNIQUE with a composite UNIQUE (conjunto_id, placa) via a new migration, matching the multi-tenant model. Keep the 409 mapping for genuine intra-conjunto duplicates.
- **Verifier:** Every load-bearing fact in the claim is real and unchanged. 1. up.sql:8 is verbatim `placa text NOT NULL UNIQUE,` inside `CREATE TABLE vehiculos`. In PostgreSQL a column-level UNIQUE is a table-wide (global) constraint; there is no conjunto_id scoping. The only conjunto-related artifacts on this table are a plain non-unique index (`vehiculos_conjunto_id_idx`, line 17). I checked all 12 migration directories plus down.sql and grepped for `ALTER TABLE vehiculos`, `DROP CONSTRAINT`, and any composite `UNIQUE (conjunto_id, placa)` — none exist. The diesel schema (schema.rs:527) likewise shows `pl

### HIGH-7 **[BLOCKER]** — Poderes (proxy votes) double-count the otorgante's coefficient — otorgante can still cast their own vote
- **Where:** `backend/api/src/domains/asamblea/handlers.rs:330-357`  · _correctness / domains-realtime-asamblea_ · confidence: high
- **Why:** compute_effective_coeficiente (repo.rs:241-299) returns the apoderado's OWN unidad_id plus a coeficiente that is base (own unit) + the SUM of every otorgante's unit coefficient. cast_voto stores that combined weight against the apoderado's own unidad_id. The DB unique constraint is asamblea_votos_unico_por_unidad UNIQUE (votacion_id, unidad_id) — keyed on the apoderado's unit, NOT the otorgantes' units. Nothing prevents each otorgante from independently casting their own vote (their own unidad_id, which is distinct), so the otorgante's coefficient is counted twice: once inside the apoderado's aggregated vote and once in the otorgante's own vote row. Both rows satisfy the unique constraint. This corrupts every tallied result and quorum where a poder exists.
- **Fix:** Record one voto row per unit (apoderado vote should produce one row per represented unidad_id, each with that unit's own coeficiente), OR block the otorgante from voting once a verified poder exists for them in this asamblea (and vice-versa). The unique (votacion_id, unidad_id) constraint then actually prevents the otorgante's unit from being counted twice. Tally by summing per-unidad rows, never a pre-aggregated coefficient stored on the apoderado's unit.
- **Verifier:** The cited code is real and unchanged. handlers.rs:330-357 calls compute_effective_coeficiente then cast_voto exactly as quoted. compute_effective_coeficiente (repo.rs:241-299) returns the apoderado's OWN unidad_id (lines 247-251, returned at 298) and a coeficiente of base + poder_coef, where base is the apoderado's own unit coefficient and poder_coef is the SUM of every verified otorgante's unit coefficient (lines 273-296). This combined weight is stored against the apoderado's own unidad_id. The unique constraint (migration 2026-06-10-000011_asamblea/up.sql:119) is asamblea_votos_unico_por_un

### HIGH-8 **[BLOCKER]** — Voters without a unit bypass the one-vote-per-unit constraint (NULL unidad_id) and get a fabricated 0.01 coefficient
- **Where:** `backend/api/src/domains/asamblea/handlers.rs:331-343`  · _correctness / domains-realtime-asamblea_ · confidence: high
- **Why:** If the user has no unidad (unidad_id = None), cast_voto inserts a voto with unidad_id = NULL. The unique constraint asamblea_votos_unico_por_unidad UNIQUE (votacion_id, unidad_id) does NOT dedupe NULLs in Postgres (NULL != NULL), so a unit-less user can POST /votaciones/{id}/votos unlimited times — each call inserts a new row. On top of that, the explicit fallback assigns every such vote a coeficiente of 0.01, injecting voting weight for users who legitimately have zero. Together this lets a single unit-less account stuff a ballot with arbitrary weight.
- **Fix:** Reject the vote (400/409) when unidad_id is None, and enforce one vote per (votacion_id, usuario_id) as well via a partial unique index covering NULL unidad_id. Remove the 0.01 fallback — a zero-coefficient user must not silently acquire weight; either reject or store 0.
- **Verifier:** The quoted code is real and unchanged. In backend/api/src/domains/asamblea/handlers.rs the cast_voto handler (lines 308-373) calls repo::compute_effective_coeficiente, which returns (Option<Uuid>, BigDecimal). At lines 334-339 a zero coeficiente is replaced with BigDecimal::from(1)/BigDecimal::from(100) = 0.01. The unidad_id (Option<Uuid>) flows unchanged into NuevoVoto and into the INSERT. repo.rs:231-238 cast_voto is a plain diesel insert_into with no upsert/on_conflict. repo.rs:241-262 compute_effective_coeficiente reads usuarios.unidad_id, which is Option<Uuid>; when the user has no unit

### HIGH-9 **[BLOCKER]** — create_poder accepts arbitrary otorgante_id/apoderado_id with no tenant or membership validation
- **Where:** `backend/api/src/domains/asamblea/handlers.rs:591-613`  · _tenancy / domains-realtime-asamblea_ · confidence: high
- **Why:** The handler verifies the asamblea belongs to the caller's conjunto but never checks that req.otorgante_id and req.apoderado_id are usuarios in the same conjunto, nor that the caller is the otorgante or an admin. Any authenticated resident can forge a poder naming any user UUID as otorgante and themselves as apoderado. Once an admin later flips verificado=true (or if any path sets it), compute_effective_coeficiente and get_quorum (repo.rs:264-296, 366-385) sum that otorgante's unit coefficient into the attacker's vote/quorum. The only DB guards are otorgante<>apoderado and one poder per otorgante per asamblea — neither prevents naming a foreign-tenant or arbitrary user. This is both a vote-weight forgery and a potential cross-tenant coefficient leak.
- **Fix:** In create_poder, require the caller to be the otorgante (or an admin), and validate both otorgante_id and apoderado_id exist in usuarios WHERE conjunto_id = user.conjunto_id before insert. Reject otherwise with 400/403.
- **Verifier:** The quoted code is real and unchanged. handlers.rs:591-628 create_poder only calls repo::verify_asamblea_tenant(asamblea_id, user.conjunto_id) (verifying the ASAMBLEA belongs to the caller's conjunto) and checks documento_url is non-empty. It does NOT call guard::require(&user, ADMIN_ROLES) and does NOT validate that req.otorgante_id / req.apoderado_id are usuarios in the caller's conjunto, nor that the caller is the otorgante. Contrast: the sibling update_poder handler (handlers.rs:637) DOES have guard::require(&user, ADMIN_ROLES). The router (handlers.rs:65-67) wires .post(create_poder) with

### HIGH-10 **[BLOCKER]** — Storage falls back to FakeStorage in production and persists fake localhost URLs (Law 4 violation)
- **Where:** `backend/api/src/services/storage.rs:85-97, 100-133`  · _production / services_ · confidence: high
- **Why:** create_storage_service is called from AppState::new (src/state.rs:23), the real production constructor. If S3_ENDPOINT/bucket/keys are not set, it returns FakeStorage, whose upload() does NO I/O and returns a fabricated http://localhost:9000/audio/<path> URL with Ok. In src/domains/chat/handlers.rs:74-79 and :223-226 that fake URL is stored in the DB (audio_url) and broadcast to clients as if a real audio file exists. Residents' voice messages are silently lost while the API reports success — a mock fallback masking an unconfigured dependency, which Law 4 forbids (should be 503). Same S3 init failure path (line 125-128) logs the error but still falls through to FakeStorage instead of surfacing the misconfiguration.
- **Fix:** In production, when no S3 config is present (or S3Storage::new fails), do not substitute FakeStorage. Either store None and have upload paths return ApiError::ServiceUnavailable, or gate FakeStorage behind a test-only cfg/flag. Audio uploads that cannot be persisted must fail loudly (503), not return a dangling localhost URL that gets written to the DB and pushed over the websocket.
- **Verifier:** All cited code is real and unchanged. storage.rs:85-97 — FakeStorage::upload ignores the data bytes (`_data: &[u8]`), builds `format!("http://localhost:9000/{bucket}/{path}")` and returns `Ok(url)` with zero I/O. storage.rs:100-133 — create_storage_service only returns S3Storage when ALL FOUR of s3_endpoint/s3_bucket/s3_access_key/s3_secret_key are Some; otherwise (or if S3Storage::new errors at line 125-128, which only logs via tracing::error! and falls through) it returns Arc::new(FakeStorage) after a tracing::warn!. The production wiring is confirmed: main.rs:24 calls AppState::new (the re

### HIGH-11 **[BLOCKER]** — No request timeout layer — slow upstreams (Gemini/S3/DB) can pin the pool and worker tasks indefinitely
- **Where:** `backend/api/src/lib.rs:47-57`  · _production / backend-infra_ · confidence: high
- **Why:** Cargo.toml enables tower-http's `timeout` feature, but `build_router` never applies a `TimeoutLayer`. There is no upper bound on how long a request can run. Handlers call out to Gemini, S3, web-push, LiveKit, and Postgres; if any of those hang (TCP black-hole, slow DB), the corresponding request hangs forever, holding its DB connection (see the no-pool-timeout finding) and a worker task. Under load this cascades into pool exhaustion and an unresponsive service that health checks may still report as 'ok' for liveness. Production HTTP services must bound request duration.
- **Fix:** Add `.layer(TimeoutLayer::new(Duration::from_secs(30)))` (the feature is already enabled) so requests fail with 408/503 instead of hanging. Pair with per-call timeouts on outbound HTTP clients (Gemini/S3/push) and on `pool.get()`.
- **Verifier:** The quoted code is real and verbatim. build_router (backend/api/src/lib.rs:24-57) applies exactly three layers — cors, RequestBodyLimitLayer, TraceLayer — and NO TimeoutLayer. Cargo.toml enables tower-http feature "timeout" (line: features = ["cors", "trace", "timeout", "limit"]) but a grep for "TimeoutLayer/timeout/Timeout" across api/src returns only two hits, neither of which is a request-layer timeout: db/mod.rs:83 (a 3s timeout wrapping pool.get(), but ONLY inside ping() used by /healthz) and gemini.rs:60 (a 30s reqwest client timeout). main.rs uses axum::serve with no timeout. So there i

### HIGH-12 — Logout and password change do not invalidate outstanding JWTs; 30-day tokens remain valid after sign-out
- **Where:** `backend/api/src/domains/auth_routes.rs:113-120`  · _security / auth-tenancy_ · confidence: high
- **Why:** JWTs are stateless and signed with a 30-day exp (jwt.rs SESSION_DAYS=30); there is no server-side session store, no jti/denylist, and no token-version claim. logout only clears the ec_session cookie. A token already captured (e.g. the Bearer copy the frontend persists in localStorage, useAuth.ts:32) stays valid for up to 30 days after the user logs out. change_password (auth_routes.rs:139-164) likewise updates the hash but does not bump any token version, so a stolen pre-change token keeps working. For a multi-tenant app holding payments, assembly votes, and resident PII, an unrevocable 30-day credential is a meaningful account-takeover persistence window.
- **Fix:** Add a server-side revocation mechanism: e.g. a token_version / password_changed_at column on usuarios included as a claim and re-checked on each request, or a short access-token + refresh-token scheme. At minimum, invalidate sessions on password change. Shorten access-token lifetime well below 30 days.
- **Verifier:** Every element of the claim holds up against the actual code. 1. Quoted code is real and unchanged. backend/api/src/domains/auth_routes.rs:113-120 is verbatim: logout() only builds a removal cookie via session_cookie(String::new(), ...).make_removal() and adds it to the jar. It performs no server-side action — no DB write, no revocation. 2. Tokens are stateless with a 30-day exp. backend/api/src/auth/jwt.rs:10 defines SESSION_DAYS = 30; issue() sets exp = iat + 30 days (line 38). The Claims struct (lines 15-22: sub, conjunto_id, rol, nombre, iat, exp) has NO jti and NO token-version field. ve

### HIGH-13 — Bearer token stored in localStorage (XSS-exfiltratable) defeats the HttpOnly cookie
- **Where:** `src/hooks/useAuth.ts:30-32`  · _security / auth-tenancy_ · confidence: high
- **Why:** The backend correctly sets the session cookie HttpOnly (auth_routes.rs:169) so script cannot read it, but the login response also returns the raw JWT (LoginResponse.token) which the frontend persists in localStorage and replays as an Authorization: Bearer header (client.ts:67-71, WebSocketProvider.tsx:28). localStorage is readable by any injected script, so a single XSS anywhere in the app exfiltrates a full 30-day, non-revocable (finding #3) tenant credential - negating the HttpOnly protection. The WS handler also takes the token as a URL query param (ws.rs:13-16), which lands in proxy/server logs and browser history.
- **Fix:** Prefer the HttpOnly cookie exclusively once the frontend and API share a site/domain, and stop returning/persisting the raw token. If a cross-site bearer fallback is truly required during migration, gate it behind the cookie_cross_site config and scope its lifetime tightly. For WS auth, pass the token via a Sec-WebSocket-Protocol header or a short-lived ticket rather than the query string.
- **Verifier:** All cited code is real and unchanged. useAuth.ts:30-32 stores the raw JWT in localStorage via localStorage.setItem('ec_token', res.token) — exact match to the quote. The backend genuinely sets the session cookie HttpOnly: auth_routes.rs:169 cookie.set_http_only(true) (in session_cookie()). The login handler returns the SAME raw JWT in the response body: auth_routes.rs:80-87 builds session_cookie(token.clone()) AND Json(LoginResponse { user, token }); types.ts:84-88 confirms token: string. So the credential protected by HttpOnly on the cookie is simultaneously handed to JS. client.ts:67-71 read

### HIGH-14 — Double-booking race condition: reservation overlap check is not serializable and has no DB-level exclusion constraint
- **Where:** `backend/api/src/domains/reservas/repo.rs:97-148`  · _correctness / domains-operational_ · confidence: high
- **Why:** The overlap check and the insert run inside a transaction, but a plain count() takes no row lock and the connection uses the default READ COMMITTED isolation (no Serializable / SELECT ... FOR UPDATE is set anywhere in the codebase). Two concurrent crear_reserva calls for the same area and overlapping window can both run the count, both see 0 overlaps, and both insert, producing a double-booking. The migration backend/migrations/2026-06-10-000005_reservas/up.sql defines only a btree index reservas_area_inicio_idx and a fecha range CHECK; there is NO `EXCLUDE USING gist (...)` constraint, so the database cannot reject the second overlapping row either. The transaction wrapper gives atomicity but not isolation against this phantom.
- **Fix:** Add a Postgres exclusion constraint on reservas, e.g. `CREATE EXTENSION btree_gist; ALTER TABLE reservas ADD CONSTRAINT reservas_no_overlap EXCLUDE USING gist (area_id WITH =, tstzrange(fecha_inicio, fecha_fin) WITH &&) WHERE (estado <> 'CANCELADA');` and map the resulting exclusion-violation DB error to ApiError::Conflict. Alternatively run the transaction at SERIALIZABLE isolation and retry on serialization failure, or take a row lock on the area. The DB constraint is the most robust.
- **Verifier:** The quoted code at backend/api/src/domains/reservas/repo.rs:97-148 is real and unchanged: inside conn.transaction(), crear_reserva runs a plain count() overlap check (lines 109-117) with the half-open predicate fecha_inicio < fecha_fin AND fecha_fin > fecha_inicio, returns Conflict if >0, then diesel::insert_into (lines 130-142). The overlap logic itself is correct (standard interval-overlap test, with proper conjunto_id and area_id filters — no missed guard). Isolation/locking claim verified: grep across the entire backend for build_transaction, serializable, repeatable, isolation, read_comm

### HIGH-15 — No transactions: partial failure leaves the target DB in an inconsistent, half-migrated state
- **Where:** `backend/migrate/src/steps.rs:118-478`  · _data-integrity / migrate-binary_ · confidence: high
- **Why:** Every INSERT is issued via `target.execute(...)` in autocommit mode; there is no `target.transaction()` / BEGIN wrapping a phase or the whole run. If the process dies or a phase errors partway (e.g. connection drop after migrating 5000 of 10000 usuarios), the DB is left half-populated. Because child phases (notificaciones, visitas, paquetes, vehiculos, etc.) derive conjunto_id by looking up the *target* usuarios table, a half-migrated parent set causes those children to be silently skipped (skipped += 1) rather than failing loudly. Re-running is the recovery path, but combined with the silent-skip behavior the operator gets no signal that rows are missing.
- **Fix:** Wrap each phase (ideally each whole run when feasible) in a transaction so a phase is all-or-nothing, or at minimum document that a phase must complete before dependent phases run and make `--verify` detect the resulting under-counts as errors rather than ignoring derived-skip rows.
- **Verifier:** The claim is accurate on every checkable point. (1) grep for transaction/BEGIN/COMMIT/ROLLBACK in backend/migrate/src returns NONE FOUND. db.rs::connect returns a plain tokio_postgres::Client (autocommit) with no transaction setup. run_all (steps.rs:118-478) and its caller main.rs:72 issue every INSERT via target.execute(...) with no target.transaction() wrapping any phase or the whole run. (2) Partial-failure inconsistency is real: per-row insert Errs only increment `errors` and continue, but the per-row conjunto_id lookups use `?` (e.g. query_opt at lines 724, 784, 847, 907, 968, 1081, 1143)

### HIGH-16 — --seed-demo has no production guard and can run against the prod database
- **Where:** `backend/migrate/src/main.rs:42-51`  · _production / migrate-binary_ · confidence: high
- **Why:** The task requires `--seed-demo` cannot run against prod. There is no environment/host check, no confirmation prompt, no `--force`, and it falls back to `DATABASE_URL` (the normal app connection string a developer is most likely to have exported). Running `enconjunto-migrate --seed-demo` in any shell that has DATABASE_URL pointed at production will inject a 'Conjunto Demo' tenant plus admin/residente/vigilante accounts with known hardcoded passwords (Admin123!, Residente123!, Vigilante123!) into production (seed.rs:21-51). Those are live, loginable credentials in a real conjunto's environment.
- **Fix:** Refuse to seed unless an explicit opt-in is present (e.g. require ENCONJUNTO_ALLOW_SEED=1 AND that the target host/subdomain is non-prod), or detect prod by checking for existing non-demo conjuntos and abort. At minimum require a separate SEED_DATABASE_URL rather than silently using DATABASE_URL.
- **Verifier:** The quoted code at backend/migrate/src/main.rs:42-51 is real and verbatim. target_url resolves MIGRATIONS_DATABASE_URL or falls back to DATABASE_URL (lines 42-44), and `if args.seed_demo` connects to that target and calls seed::seed_demo with no environment/host/--force/confirmation guard, then returns (lines 47-51). I confirmed seed.rs:8-54: it INSERTs a 'Conjunto Demo' conjunto plus admin@demo.conjuntos.app, residente@demo.conjuntos.app, vigilante@demo.conjuntos.app with hardcoded passwords Admin123!, Residente123!, Vigilante123! Argon2id-hashed (hash_password, lines 57-64), all with must_ch

### HIGH-17 — --verify only compares COUNT(*); it does not check FK integrity or field equality and produces false mismatches
- **Where:** `backend/migrate/src/verify.rs:43-61`  · _correctness / migrate-binary_ · confidence: high
- **Why:** The task and spec (spec.md:13 '--verify (row counts + spot checks)') require row counts AND FK integrity AND spot-check field equality. verify.rs does only COUNT(*) per table. It performs no JOIN/orphan check (e.g. notificaciones.usuario_id with no matching usuarios row) and no field-level comparison of any migrated value, so the silent decimal-zeroing and any field corruption pass verification undetected. Conversely, because many phases legitimately skip rows (derived conjunto_id missing, asamblea_pairings with no usuario, email collisions), target counts will be LOWER than legacy for those tables and verify will report them as mismatches with no way to distinguish an expected skip from real data loss — making the report noisy and untrustworthy.
- **Fix:** Add orphan/FK checks (LEFT JOIN ... WHERE parent IS NULL) for each child table, add spot-check field equality on a sampled set of ids (especially decimal/json columns), and account for intentionally-skipped rows so genuine losses are distinguishable from by-design skips.
- **Verifier:** The quoted code at backend/migrate/src/verify.rs:45-61 is real and unchanged. verify() iterates TABLE_PAIRS and runs only SELECT COUNT(*) on legacy ({leg}) and target ({tgt}), pushing a mismatch when lc != tc. There is no JOIN, no orphan/FK check, and no field-level comparison. main.rs:57-66 confirms args.verify calls ONLY verify::verify — there is no other verification helper or guard the reviewer missed. The spec (specs/014-data-migration/spec.md:13) requires '--verify (row counts + spot checks)', and main.rs:24 itself documents the flag as 'Compare row counts and spot-check field equality',

### HIGH-18 — JWT stored in localStorage — XSS can exfiltrate a 30-day session token
- **Where:** `src/hooks/useAuth.ts:31-33`  · _security / frontend-auth-client_ · confidence: high
- **Why:** The backend already sets an httpOnly `ec_session` cookie (auth_routes.rs line 169 `cookie.set_http_only(true)`), which the API client always sends via `credentials: 'include'`. The login flow ALSO mirrors the raw JWT into `localStorage['ec_token']`, and the client (client.ts line 68) and WebSocketProvider (line 28) read it back. localStorage is readable by any JavaScript on the page, so any XSS (this app renders user-supplied chat, anuncios, classifieds, base64 images, and even uses `dangerouslySetInnerHTML` on the login page) can steal the token. The token is a 30-day JWT (jwt.rs `SESSION_DAYS = 30`) with no server-side revocation, so a stolen token grants a month of full account access including the user's `conjunto_id` and `rol`. The httpOnly cookie path is immune to this; the localStorage copy defeats that protection.
- **Fix:** Drop the localStorage token for normal operation and rely solely on the httpOnly cookie. The only consumer that genuinely needs the raw token is the WebSocket (browsers can't attach the cookie to a cross-origin WS handshake cleanly) — for that, mint a short-lived (e.g. 60s) single-use WS ticket from an authenticated `GET /auth/ws-ticket` endpoint instead of persisting the long-lived JWT client-side. If the cross-site Bearer fallback must stay during migration, scope it tightly and shorten the token lifetime.
- **Verifier:** The quoted code is real and unchanged. /Users/nkt/dev/EN-CONJUNTO/src/hooks/useAuth.ts:31-33 mirrors the raw JWT into localStorage['ec_token'] exactly as quoted (the actual file lives at repo root src/, not backend/src/ as the claim header stated). This is live code, not dead: the login handler (backend/api/src/domains/auth_routes.rs:34-46) issues the JWT, sets it as an httpOnly cookie (line 169 cookie.set_http_only(true), confirmed), AND returns the same token in the LoginResponse JSON body (line 46), which useAuth stores. The token is read back in two places: the API client (src/lib/api/clie


## Medium (12)

### MEDIUM-1 — No rate limiting or lockout on login / password / pairing-PIN endpoints enables brute-force
- **Where:** `backend/api/src/lib.rs:47-57`  · _security / auth-tenancy_ · confidence: high
- **Why:** The router applies only CORS, a body-size limit, and tracing - there is no rate-limiting/throttling layer (confirmed: no tower_governor/governor/tower::limit anywhere in the crate). POST /auth/login, PUT /auth/password, and the asamblea pairing PIN check (get_pairing brute-forces a PIN by verifying against every pending pairing) are all unthrottled. Login does implement constant-time user-enumeration mitigation (dummy_hash) which is good, but offers no protection against online password guessing. The short assembly PIN is especially brute-forceable with no attempt cap.
- **Fix:** Add per-IP and per-account rate limiting / exponential backoff (e.g. tower_governor) on auth and pairing endpoints, plus temporary lockout after repeated failures. Treat the pairing PIN flow as a credential and cap verification attempts.
- **Verifier:** The quoted router in backend/api/src/lib.rs:47-57 is real and unchanged: it applies only cors, RequestBodyLimitLayer(2 MiB), and TraceLayer — no rate-limiting/lockout layer. A grep across all .rs/.toml for governor/tower_governor/ratelimit/throttle/lockout/failed_attempt/max_attempts returned zero hits, confirming none exists anywhere in the crate. The three cited sinks behave as described: - POST /auth/login (auth_routes.rs:50-88): fully public, unthrottled, no attempt cap. It does implement the constant-time user-enumeration mitigation via dummy_hash() (lines 58-63, 181-186) exactly as the

### MEDIUM-2 — pagar() performs no status-transition validation, allows re-paying or paying disputed/voided charges
- **Where:** `backend/api/src/domains/pagos/repo.rs:61-84`  · _correctness / domains-operational_ · confidence: high
- **Why:** The update filters by id/conjunto/usuario but not by current estado. The pagos_estado_check allows PENDIENTE, PAGADO, VENCIDO, EN_DISPUTA. As written, a pago already in PAGADO can be 'paid' again (overwriting fecha_pago and metodo), and a pago in EN_DISPUTA can be silently flipped to PAGADO, bypassing the dispute. Idempotency/state-machine integrity for money is not enforced. There is no filter restricting which estados may legally transition to PAGADO.
- **Fix:** Add `.filter(pagos::estado.eq_any(vec![EstadoPago::Pendiente, EstadoPago::Vencido]))` (or at minimum exclude PAGADO and EN_DISPUTA) so the update is a no-op (returns None -> 404/409) when the charge is not in a payable state. Consider returning 409 Conflict with a clear message rather than 404 when the pago exists but is not payable.
- **Verifier:** The quoted code at backend/api/src/domains/pagos/repo.rs:61-84 is real and verbatim-accurate. `pagar()` issues a `diesel::update` filtered only by `pagos::id`, `pagos::conjunto_id`, and `pagos::usuario_id` (ownership/tenancy guards), then unconditionally sets `estado = Pagado`, `fecha_pago = now()`, `metodo`. There is NO `.filter(pagos::estado.eq(...))` and no current-estado precondition anywhere. I checked for a missed guard in three places: 1. The handler (handlers.rs:59-83) calls `repo::pagar` directly and only maps a `None` result to a 404 — no estado pre-check, no role guard beyond `Auth

### MEDIUM-3 — Pairing PIN verification endpoint has no brute-force / rate-limit protection
- **Where:** `backend/api/src/domains/asamblea/handlers.rs:158-179`  · _security / domains-realtime-asamblea_ · confidence: high
- **Why:** GET /asambleas/pairing?pin=XXXXXX brute-forces a PIN against every pending pairing in the conjunto with no attempt counter, lockout, or rate limit. create_pairing (line 190) defaults expires_minutes to 5 but the PIN space is whatever the venue screen generates (legacy was a 6-digit numeric = 1e6). A unit-less or any authenticated attacker can iterate the PIN space and, on a match, link_pairing binds the pairing to the attacker's own user id (line 177), hijacking the device-pairing session. There is also no per-pairing failed-attempt invalidation.
- **Fix:** Add per-(conjunto, user/IP) rate limiting and a failed-attempt counter that expires the pairing after N misses. Prefer a longer/alphanumeric PIN and short TTL. Optionally make pairing a single-target lookup keyed by a pairing id rather than scanning all pending rows.
- **Verifier:** The quoted code at handlers.rs:158-179 is real and unchanged: get_pairing loops over all pending pairings, calls password::verify_password_blocking(query.pin, pairing.pin_hash) for each, and on match calls repo::link_pairing(pairing.id, user.id) binding the pairing to the caller. create_pairing (handlers.rs:190) does default expires_minutes to 5. The core security claim holds: 1. NO rate limiting exists anywhere — grep for rate.?limit|governor|throttle|ConcurrencyLimit across all .rs/.toml returned zero hits. The global layer stack (lib.rs:54-56) is only CORS + RequestBodyLimit + TraceLayer.

### MEDIUM-4 — Residents can self-register attendance and cast votes with no active-session or admin gate
- **Where:** `backend/api/src/domains/asamblea/handlers.rs:397-417`  · _correctness / domains-realtime-asamblea_ · confidence: medium
- **Why:** register_asistencia hard-codes verificado: true and takes ip/dispositivo straight from the client body, so the 'verified attendance' flag is meaningless — any resident self-marks as verified-present, directly inflating get_quorum. cast_voto (line 308) never checks the user is registered as present, nor that the asamblea/session is active (only votacion.activa). Combined with the quorum being the gate for assembly decisions, attendance and quorum integrity rest entirely on unauthenticated client claims.
- **Fix:** Do not trust client-supplied verificado/ip/dispositivo; set verificado only via an admin/verified-pairing path and capture IP server-side. Gate cast_voto on an existing asistencia row and on the asamblea being activa.
- **Verifier:** The quoted code is real and unchanged. handlers.rs:397-417 (register_asistencia) calls only repo::verify_asamblea_tenant (a tenant/conjunto isolation check, NOT a role check) and then hard-codes verificado: true while taking req.ip and req.dispositivo straight from the client body (confirmed in dto.rs:205-211, CreateAsistenciaRequest has client-supplied ip/dispositivo). The DB migration (migrations/.../000011_asamblea/up.sql:73) defaults verificado to false by design, so the handler's hard-coded true makes the verification flag meaningless for any self-registration. There is NO admin guard: co

### MEDIUM-5 — WebSocket clients silently dropped on broadcast lag with no resync (backpressure)
- **Where:** `backend/api/src/domains/ws.rs:45-61`  · _correctness / services_ · confidence: high
- **Why:** tokio::sync::broadcast::Receiver::recv() returns Err(RecvError::Lagged(n)) when a slow consumer falls more than 256 events behind, and Err(RecvError::Closed) when the channel closes. The `while let Ok(event)` pattern treats BOTH errors as loop termination. On Lagged — which is recoverable and just means events were skipped — the send_task exits, the select! aborts recv_task, and the client is silently disconnected mid-session with no error frame and no way to know it missed events. Under a burst (e.g. many publishes during an active asamblea, where asamblea/handlers.rs publishes on nearly every mutation) a temporarily slow client gets booted rather than catching up. There is also no application-level resync, so the frontend's view is left stale until it reconnects.
- **Fix:** Match on recv() explicitly: on Ok(event) forward it; on Err(Lagged(n)) log/optionally send a 'resync' hint to the client and `continue` the loop (do not break); only break on Err(Closed). Consider raising CHANNEL_CAPACITY or sending a typed 'lagged, please refetch' event so the frontend can reload affected domains.
- **Verifier:** The quoted code is real and unchanged. ws.rs:40-79 (handle_socket) contains exactly `let mut rx = hub.subscribe(conjunto_id).await;` followed by `while let Ok(event) = rx.recv().await { ... if sender.send(Message::Text(json.into())).await.is_err() { break; } }` inside a spawned send_task. ws_hub.rs:9 defines `const CHANNEL_CAPACITY: usize = 256;` and subscribe() (lines 56-58) returns a `tokio::sync::broadcast::Receiver<WsEvent>`, confirming `rx` is exactly the broadcast receiver the reviewer describes. The doc comment at ws_hub.rs:8 literally says "Max events buffered per channel before slow r

### MEDIUM-6 — Gemini API key passed in URL query string (secret-in-URL logging risk)
- **Where:** `backend/api/src/services/gemini.rs:74-77`  · _security / services_ · confidence: medium
- **Why:** The Gemini API key is embedded in the request URL as ?key=<secret>. While this is Google's documented auth scheme, URLs are far more likely than headers to be captured in logs, proxy access logs, tracing spans, and error contexts. reqwest errors (e.g. timeout, connect failure) returned via the `?` operator on send() can include the full URL with the key, and that anyhow error is then formatted into ApiError::Upstream(format!("Error del servicio de IA: {e}")) in ai/handlers.rs and surfaced. Note the BAD_GATEWAY/Upstream response body is generic, but any debug/tracing of the underlying anyhow error would expose the key. The bail! on non-success status only logs status+body (safe), but the transport-error path is the exposure.
- **Fix:** Prefer the x-goog-api-key request header over the ?key= query param so the secret never appears in the URL. At minimum, ensure reqwest transport errors are not logged at debug with the full URL, and scrub the key before formatting any error that includes the request URL.
- **Verifier:** All cited code is real and unchanged. gemini.rs:74-77 builds the URL with `?key={self.api_key}` (key sourced from env var GEMINI_API_KEY, config.rs:76 — a genuine secret). gemini.rs:91 does `.send().await?`; on a transport error (timeout/connect/TLS/DNS) reqwest 0.12.28 attaches the request URL to the error (verified in reqwest async_impl/client.rs:3048-3071 via with_url/if_no_url), and reqwest's Display impl (error.rs:267-268) appends ` for url ({url})`. The url crate 2.5.8 Display (lib.rs:2797-2799) emits the full serialization including the `?key=SECRET` query param with NO redaction (it on

### MEDIUM-7 — No graceful shutdown — SIGTERM drops in-flight requests and open DB connections
- **Where:** `backend/api/src/main.rs:27-30`  · _production / backend-infra_ · confidence: high
- **Why:** `axum::serve(...).await` is used without `.with_graceful_shutdown(...)`. No SIGTERM/SIGINT handler exists anywhere in the crate (grep for `signal`/`ctrl_c`/`graceful_shutdown` returns nothing). When an orchestrator (Docker/K8s/Fly) sends SIGTERM during a rolling deploy, the process is killed immediately: in-flight requests are aborted mid-write, clients see connection resets, and pooled Postgres connections are not closed cleanly. This causes user-visible errors on every deploy and can leave brief connection churn on the DB.
- **Fix:** Wrap with `axum::serve(listener, app).with_graceful_shutdown(shutdown_signal())` where `shutdown_signal` awaits `tokio::signal::ctrl_c()` and (on Unix) SIGTERM, allowing in-flight requests to drain before exit.
- **Verifier:** The quoted code at backend/api/src/main.rs:27-30 is real and exactly matches: TcpListener::bind, tracing::info!, then `axum::serve(listener, app).await?` followed by `Ok(())`. There is NO `.with_graceful_shutdown(...)` chained onto serve. I grepped all 122 .rs files in the crate for `graceful`, `ctrl_c`, `signal`, `SIGTERM`, `SIGINT`, `tokio::signal` and got ZERO matches — confirming no SIGTERM/SIGINT handler exists anywhere. `axum::serve` is used only at this single site (api/src/main.rs:29). The stack supports the fix: axum 0.8 (which has with_graceful_shutdown) and tokio with `features = ["

### MEDIUM-8 — No panic-catch layer — a handler panic aborts the connection instead of returning a clean 500
- **Where:** `backend/api/src/lib.rs:54-56`  · _production / backend-infra_ · confidence: high
- **Why:** There is no `tower_http::catch_panic::CatchPanicLayer` (grep confirms `CatchPanic` appears nowhere). The error module maps `anyhow`/diesel errors to RFC-7807 500s, but that only covers `Result::Err` paths. An actual panic inside a handler (unwrap on a None, slice out of bounds, `.lock().unwrap()` on a poisoned mutex like in RecordingPushSender, arithmetic overflow, etc.) is not caught: the spawned connection task unwinds and the client receives a dropped/reset connection rather than a 500 problem+json. This is both a worse client contract and an observability gap.
- **Fix:** Add `.layer(CatchPanicLayer::custom(...))` that logs the panic and returns a 500 `application/problem+json` body, keeping the panic-to-500 contract consistent with `ApiError::Internal`.
- **Verifier:** All elements of the claim verified against the actual code. (1) The quoted code at backend/api/src/lib.rs:54-56 is real and unchanged: .layer(cors) / .layer(RequestBodyLimitLayer::new(MAX_BODY_BYTES)) / .layer(TraceLayer::new_for_http()), with no other layers on the router. (2) grep -rn "CatchPanic\|catch_panic" over all .rs files returns ZERO hits (exit 1) — no CatchPanicLayer present. Furthermore api/Cargo.toml:38 declares tower-http with features ["cors","trace","timeout","limit"] only — the "catch-panic" feature is not even enabled, so the layer could not be added without a Cargo change. (

### MEDIUM-9 — Required legacy timestamps that fail to decode are silently written as the Unix epoch (1970-01-01)
- **Where:** `backend/migrate/src/steps.rs:47-51`  · _data-integrity / migrate-binary_ · confidence: high
- **Why:** `unwrap_or_default()` on a DateTime yields 1970-01-01T00:00:00Z. Any required timestamp column (creadoEn, fecha, fechaVencimiento, publicadoEn, etc.) that fails to decode — e.g. a NULL in a column the new schema treats as NOT NULL, or a type the two FromSql attempts don't cover — is silently inserted as the epoch instead of erroring or being reported. This corrupts ordering/aging logic (payment due dates, announcement publish dates) without any report row, unlike the JSON path which at least records a ReportRow.
- **Fix:** For required timestamps, treat a decode failure as an error (increment errors / push a ReportRow) rather than defaulting to epoch, so operators can see and fix the affected rows.
- **Verifier:** The quoted code at backend/migrate/src/steps.rs:47-51 is real and unchanged: `ts()` does `try_get::<DateTime<Utc>>` then falls back to `try_get::<NaiveDateTime>().map(|n| n.and_utc())`, then `.unwrap_or_default()`. chrono is 0.4.45 (Cargo.lock), and `DateTime<Utc>::default()` returns the Unix epoch (1970-01-01T00:00:00Z) in chrono 0.4 — so the load-bearing technical fact is correct. The mechanism is confirmed by the call sites: `ts()` feeds NOT-NULL target columns across ~40 usages (e.g. line 1529 `fecha_vencimiento`, line 1532/1580 `created_at`/`fecha`, plus `publicadoEn`, `fechaInicio`, `fe

### MEDIUM-10 — No global 401 handler: expired/revoked session leaves stale user state, no auto-logout or redirect
- **Where:** `src/lib/api/client.ts:82-96`  · _ux / frontend-auth-client_ · confidence: high
- **Why:** When the session expires or the token is rejected, every API call throws `ApiError(401)`, but nothing in the client or `useAuth` reacts to a 401 globally. The Zustand `user` state stays populated (it was set at login / checkAuth and is only cleared by explicit `logout()` or a failed `checkAuth()` on mount). Middleware only redirects on cookie ABSENCE, and an expired-but-present cookie still passes middleware. The result: after the 30-day token expires (or is invalidated), a user who keeps the tab open sees a logged-in shell where every action silently fails with a toast, until they hard-refresh (which re-runs `checkAuth`, gets 401, and clears state). There is no refresh-token mechanism either, so sessions just hard-die at 30 days.
- **Fix:** In `apiFetch`, when `response.status === 401` (and the path isn't `/auth/login`), centrally clear auth state (`localStorage.removeItem('ec_token')`, `useAuth.setState({ user: null })`) and redirect to `/login?callbackUrl=...`. This gives a single, predictable expiry UX instead of per-call toast failures.
- **Verifier:** The cited code is real and exactly as quoted. /Users/nkt/dev/EN-CONJUNTO/src/lib/api/client.ts:82-96 throws ApiError for every non-ok status with no 401-specific handling. There is genuinely no global 401 interceptor. Supporting evidence I re-read: - src/hooks/useAuth.ts (Zustand store): `user` is set in login() (line 34) and checkAuth() (line 56) and only cleared by explicit logout() (line 50) or a failed checkAuth() (line 58). Nothing reacts to a 401 from other endpoints. - src/components/providers/AuthProvider.tsx:14-16: checkAuth() runs only on mount, so a 401 mid-session does not re-trig

### MEDIUM-11 — Session cookie is not Secure in the non-cross-site (same-site) deployment branch
- **Where:** `backend/api/src/domains/auth_routes.rs:171-178`  · _security / frontend-auth-client_ · confidence: high
- **Why:** Only the cross-site branch sets `Secure`. In the same-site production branch (`COOKIE_CROSS_SITE` unset/false — the intended end state once frontend and API share a domain, per the config comment), the `ec_session` cookie is issued WITHOUT the `Secure` attribute. On any plain-HTTP request to that domain the 30-day session cookie is transmitted in cleartext and can be sniffed/MITM'd. SameSite=Lax does not imply Secure. This is in scope because it directly governs the cookie the frontend auth relies on.
- **Fix:** Always call `cookie.set_secure(true)` regardless of the cross-site flag (production is HTTPS in both topologies). If local non-TLS dev needs an exception, gate it on an explicit dev flag rather than on `cookie_cross_site`.
- **Verifier:** The quoted code is real and unchanged. backend/api/src/domains/auth_routes.rs:166-179 defines session_cookie(): it always sets path="/", http_only=true, and max_age from session_max_age_seconds(). The branch `if config.cookie_cross_site` sets SameSite::None AND set_secure(true); the `else` branch sets ONLY SameSite::Lax — it does NOT set Secure. So in the same-site (cookie_cross_site=false) deployment, the cookie is issued without the Secure attribute. Confirmed this governs the real login cookie: session_cookie() is called at line 80 on successful login to issue the JWT (and line 117 for logo

### MEDIUM-12 — mustChangePassword is returned by the backend but never enforced anywhere in the frontend
- **Where:** `src/lib/api/types.ts:111`  · _contract / frontend-auth-client_ · confidence: high
- **Why:** `UserDto.mustChangePassword` is part of the login/me contract (and the backend clearly intends a forced-rotation flow, e.g. admin-provisioned residents with temporary passwords). Grepping the entire `src/` tree, `mustChangePassword` appears ONLY in the type definition — it is never read in `useAuth`, `AuthProvider`, the login page, or any guard. So a user flagged `mustChangePassword: true` logs in and goes straight to `/inicio` with full access; the temporary credential is never forced to change. This is a security-relevant gap (shared/temporary passwords persist) and a broken product contract.
- **Fix:** After login/checkAuth, if `user.mustChangePassword` is true, redirect to a mandatory change-password screen (calling `PUT /auth/password`) and block app routes until it's cleared.
- **Verifier:** Quoted code is real and unchanged: `mustChangePassword: boolean;` is at /Users/nkt/dev/EN-CONJUNTO/src/lib/api/types.ts:111 inside UserDto (note the frontend src/ lives at the repo root, not under backend/). It is a live contract field: LoginResponse carries `user: UserDto` (types.ts:85) and ProfileResponse extends UserDto (line 124); the Rust backend populates it from the DB column (dto.rs:40, models.rs:16, schema.rs:509, up.sql:46) and returns it on /auth/login (handlers.rs:41) and /auth/me. A case-insensitive grep across all of src/ shows `mustChangePassword` appears ONLY at line 111 — it i


## Low / Info (20)

### LOW-1 — ai/repo.rs acta and subtitulo queries filter only by asamblea_id with no conjunto join in the repo layer (latent)
- **Where:** `backend/api/src/domains/ai/repo.rs:13-21`  · _tenancy / auth-tenancy_ · confidence: high
- **Why:** get_acta, list_subtitulos, upsert_acta and create_subtitulo scope only by asamblea_id, with no conjunto_id filter. This is currently SAFE because every caller in ai/handlers.rs first calls asamblea_repo::verify_asamblea_tenant(asamblea_id, user.conjunto_id) before invoking these repo functions (verified at handlers.rs:201-203, 220-276, 302-304, 315-321). Flagged as a latent risk only: the tenant guarantee lives entirely in the handler, so any future caller that forgets the verify step would read/write another tenant's acta. It is not an exploitable leak today.
- **Fix:** Optional hardening: either rename these to make the precondition explicit (e.g. get_acta_unchecked) or have them join asambleas and accept conjunto_id so the isolation is enforced in the data layer, matching the pattern used by get_votacion_with_tenant_check.
- **Verifier:** The quoted code is real and unchanged. In /Users/nkt/dev/EN-CONJUNTO/backend/api/src/domains/ai/repo.rs: - get_acta (13-21) filters only by asamblea_actas::asamblea_id.eq(asamblea_id), no conjunto join. - upsert_acta (24-54) calls get_acta then updates/inserts keyed on asamblea_id / acta.id only. - list_subtitulos (58-70) filters only by asamblea_subtitulos::asamblea_id.eq(asamblea_id). - create_subtitulo (72-82) inserts NuevoSubtitulo (asamblea_id, speaker, text) with no tenant column. None of these touch conjunto_id; the asamblea_actas/asamblea_subtitulos tables are keyed off asamblea_id onl

### LOW-2 — JWT verification uses Validation::default() with no audience/issuer binding
- **Where:** `backend/api/src/auth/jwt.rs:48-56`  · _security / auth-tenancy_ · confidence: medium
- **Why:** Validation::default() in jsonwebtoken pins algorithm HS256 and validates exp, which correctly prevents the classic alg=none / RS256->HS256 confusion attacks given the symmetric secret. However it sets no aud/iss/required_spec_claims, so the same JWT_SECRET reused in any other context (or a future service sharing the secret) would mint interchangeable tokens. Low severity given current single-service use, but worth tightening.
- **Fix:** Set explicit Validation: keep algorithms=[HS256], add set_audience/set_issuer and require exp+iat, and assert the issuer in the token. This is defense-in-depth, not an active vulnerability.
- **Verifier:** The quoted code is real and unchanged. backend/api/src/auth/jwt.rs:48-56 calls decode::<Claims> with DecodingKey::from_secret(secret.as_bytes()) and &Validation::default(), exactly as quoted. Cargo.lock confirms jsonwebtoken 9.3.1, so the reviewer's description of Validation::default() behavior is accurate: it pins algorithms to [HS256], validates exp (required_spec_claims contains "exp"), and sets no aud/iss. Because the key is a symmetric HMAC secret and the algorithm is pinned to HS256, the alg=none and RS256->HS256 confusion attacks are correctly prevented. No aud/iss binding is configured

### LOW-3 — Resident/gate visit creation accepts arbitrary client-supplied fecha with no bounds (backdated/forward-dated gate records)
- **Where:** `backend/api/src/domains/vigilancia/handlers.rs:136-159`  · _data-integrity / domains-operational_ · confidence: medium
- **Why:** Both crear_visita_vigilancia and crear_visita_residente take an optional client fecha and persist it unchanged. A resident can set fecha to any past or future timestamp. Because visitas_de_hoy and stats key off a UTC day window, a malicious/buggy client can plant visits that appear (or disappear) from the gate's 'today' view, or inject far-future entries. No validation bounds the value.
- **Fix:** Validate fecha against a sane window (e.g. not more than N days in the past, not absurdly far in the future) or, for the resident self-service path, ignore the field and always use Utc::now() with explicit scheduled-visit semantics. At minimum reject fecha far outside [now - 1d, now + 90d].
- **Verifier:** The quoted code is real and unchanged. handlers.rs line 104 (crear_visita_vigilancia) and line 154 (crear_visita_residente, the cited 136-159 range) both contain `fecha: req.fecha.unwrap_or_else(Utc::now)`. The DTOs CreateVisitaVigilanciaRequest (dto.rs:80) and CreateVisitaResidenteRequest (dto.rs:93) declare `pub fecha: Option<DateTime<Utc>>` with zero validation. The value flows verbatim into NuevaVisita.fecha (an Insertable mapping directly to the visitas.fecha column, models.rs:33) and is inserted by crear_visita (repo.rs:62) with no clamping or bounds check. The "today view" reasoning hol

### LOW-4 — listar_anuncios ignores expires_en: expired announcements are still served and still trigger resident notifications
- **Where:** `backend/api/src/domains/comunicaciones/repo.rs:16-25`  · _correctness / domains-content_ · confidence: high
- **Why:** The schema has an expires_en column (comunicaciones migration line 16) and the DTO exposes expiresEn, but the list query never filters on it (no `.filter(expires_en.is_null().or(expires_en.gt(now)))`). Expired announcements continue to appear in the board for residents. The frontend cartelera/page.tsx also does not filter on expiresEn (it only maps publicadoEn and fijado), so the expiry feature is effectively dead end-to-end. Lower severity than a leak, but it is a silent functional gap that admins will assume works.
- **Fix:** Add an expiry filter to listar_anuncios (e.g. anuncios::expires_en.is_null().or(anuncios::expires_en.gt(Utc::now()))), or have the frontend hide expired entries. Decide whether 'fijado' should override expiry.
- **Verifier:** The quoted code is real and unchanged. backend/api/src/domains/comunicaciones/repo.rs:16-25 (listar_anuncios) filters only on conjunto_id, orders by (fijado desc, publicado_en desc), limits 50, and selects all columns — there is NO predicate on expires_en. The schema/migration confirms the column exists: migrations/2026-06-10-000007_comunicaciones/up.sql:16 (`expires_en timestamptz`), schema.rs:31, models.rs:21/35, and dto.rs:20 serializes it as camelCase `expiresEn`. I searched the entire comunicaciones domain and services dir: expires_en is only ever WRITTEN on create (handlers.rs:89) and re

### LOW-5 — Announcement and PQRS/trámite notification fan-out runs one INSERT per recipient in a single transaction (unbounded N queries)
- **Where:** `backend/api/src/domains/comunicaciones/repo.rs:51-72`  · _performance / domains-content_ · confidence: high
- **Why:** crear_anuncio_con_notificaciones loads every active resident and issues one INSERT per resident inside the transaction (create_notificacion is a single-row insert, notificaciones/repo.rs line 71-82). For a large conjunto (hundreds/thousands of residents) this is N round-trips holding an open transaction and a pooled connection, serializing the publish and risking pool starvation / long lock windows. The same pattern exists for solicitudes (admins only, so bounded/small) and tramites (admins only). The announcement case is the unbounded one because it targets all residents.
- **Fix:** Batch the notification inserts into a single multi-row INSERT (diesel insert_into(...).values(vec_of_rows)) instead of a per-recipient loop, or move the fan-out to a background job after the announcement commits. The announcement row is the user-visible result; notification delivery can be asynchronous.
- **Verifier:** The quoted code is real and unchanged at backend/api/src/domains/comunicaciones/repo.rs. Lines 51-57 load every active resident (rol in [Arrendatario, Propietario], activo=true) scoped to anuncio.conjunto_id. Lines 61-71 then loop, calling create_notificacion once per resident. This whole block runs inside conn.transaction(...) (line 43). create_notificacion (notificaciones/repo.rs:63-83) is genuinely a single-row INSERT ... RETURNING via get_result — one DB round-trip per call. I grep-confirmed the only insert_into(notificaciones::table) in the codebase is this single-row helper, so no batch

### LOW-6 — No length bounds on free-text content fields (titulo/contenido/descripcion/payload); only the 2MB body limit protects the DB
- **Where:** `backend/api/src/domains/comunicaciones/handlers.rs:71-75`  · _data-integrity / domains-content_ · confidence: medium
- **Why:** Across comunicaciones, solicitudes, tramites and inmuebles, free-text fields are validated only for non-emptiness; the DB columns are unbounded `text`. The only ceiling is the global RequestBodyLimitLayer of 2MB (lib.rs line 22/55). A user can post a ~2MB titulo or descripcion, which is then fanned out verbatim into the notification message (e.g. resumen() truncates contenido to 100 chars, but solicitudes embeds the full descripcion into every admin notification: 'reporta ... : {descripcion}'). This is a minor abuse/storage-amplification vector, not a leak.
- **Fix:** Add explicit max-length checks at the handler boundary (e.g. titulo <= 200, contenido/descripcion <= a few KB) returning 400, and/or add CHECK length constraints. Truncate user text before embedding it into notification messages.
- **Verifier:** Every concrete assertion in the claim holds up against the source. 1. Cited code is real and unchanged. backend/api/src/domains/comunicaciones/handlers.rs:71-75 is exactly the quoted block: `if req.titulo.trim().is_empty() || req.contenido.trim().is_empty()` -> BadRequest("titulo y contenido son obligatorios"). Only an emptiness check, no length bound. 2. Same emptiness-only pattern across the named domains: - solicitudes/handlers.rs:59-61 -> `if req.descripcion.trim().is_empty()` only. - inmuebles/handlers.rs:61 -> `if req.titulo.trim().is_empty() || req.descripcion.trim().is_empty()`

### LOW-7 — No edit/delete authorization surface for clasificados and inmuebles; residents cannot remove or correct their own listings
- **Where:** `backend/api/src/domains/clasificados/handlers.rs:13-18`  · _ux / domains-content_ · confidence: high
- **Why:** Both clasificados and inmuebles expose only GET (list) and POST (create) routes — there is no PUT/PATCH/DELETE. A resident who publishes a classified or a property listing cannot edit price/availability or take it down, and an admin cannot moderate/remove inappropriate listings. inmuebles has estados (VENDIDO/ALQUILADO/OCULTO) in the schema and the list query specifically returns the owner's non-DISPONIBLE rows, implying a state-change endpoint was intended but is missing. This is a functional/moderation gap rather than a security defect.
- **Fix:** Add owner-or-admin-scoped PATCH/DELETE endpoints (filtered by conjunto_id AND propietario_id/usuario_id = caller, or admin role) for both domains so listings can be updated, marked sold/rented, hidden, or moderated.
- **Verifier:** The quoted code is real and unchanged. clasificados/handlers.rs:13-18 defines exactly `Router::new().route("/clasificados", get(listar_clasificados).post(crear_clasificado))` — only GET (list) and POST (create). inmuebles/handlers.rs:14 mirrors this: `Router::new().route("/inmuebles", get(listar_inmuebles).post(crear_inmueble))`. I grepped every .rs file under backend/api/src for delete/put/patch/update/moderar/ocultar/eliminar routes or handlers tied to clasificados/inmuebles/locales and found NONE. The OpenAPI registry (openapi.rs:60-63) lists exactly four operations (two listar, two crear).

### LOW-8 — bootstrap_unidad inserts a new unit on every profile save with no dedup, creating orphan units that distort quorum totals
- **Where:** `backend/api/src/domains/usuarios/repo.rs:85-103`  · _data-integrity / domains-realtime-asamblea_ · confidence: medium
- **Why:** update_profile (handlers.rs:83-94) calls bootstrap_unidad whenever the user has no unidad_id and supplies apto. There is no lookup for an existing unit with the same (conjunto_id, torre, numero), so two residents of the same apartment — or the same resident across edits with transient unidad_id states — create duplicate unidades rows. get_quorum (asamblea/repo.rs:336-341) sums coeficiente over ALL unidades in the conjunto for the total; duplicate/orphan zero-coefficient units don't change the total here, but duplicate real units (once an admin sets a coefficient on the wrong copy) and split residents-per-unit silently corrupt both total and present coefficient math.
- **Fix:** Look up an existing unit by (conjunto_id, torre, numero) and reuse it; only insert when absent. Add a UNIQUE (conjunto_id, torre, numero) constraint to make bootstrap idempotent.
- **Verifier:** The cited code is real and verbatim accurate. backend/api/src/domains/usuarios/repo.rs:85-103 (bootstrap_unidad) does a bare INSERT into unidades with coeficiente=BigDecimal::from(0) and performs NO lookup for an existing (conjunto_id, torre, numero). The DB schema confirms there is no dedup safety net: migrations/2026-06-10-000001_core/up.sql:26-36 defines unidades with only a PK on id and a non-unique index on conjunto_id (line 38) — no UNIQUE constraint on (conjunto_id, torre, numero). So duplicate units are physically possible. However, the reviewer's framing is partly wrong. The trigger

### LOW-9 — link_pairing UPDATE does not re-filter conjunto_id or expiry, relying solely on the prior scan
- **Where:** `backend/api/src/domains/asamblea/repo.rs:119-137`  · _tenancy / domains-realtime-asamblea_ · confidence: medium
- **Why:** link_pairing matches only on id + estado=PENDIENTE. It omits the conjunto_id filter and the expires_at > now() check. In the current call path the pairing_id comes from find_pending_pairings which is conjunto- and expiry-scoped, so it is presently safe. But the function is not self-defending: any future caller (or a refactor that passes an id from another source) would allow linking a pairing across tenants or after expiry. Defense-in-depth (Law 2 expects repo functions to carry conjunto_id) is not honored here.
- **Fix:** Pass conjunto_id into link_pairing and add .filter(conjunto_id.eq(...)) and .filter(expires_at.gt(now())) to the UPDATE so the guarantee holds independent of the caller.
- **Verifier:** The cited code at backend/api/src/domains/asamblea/repo.rs:119-137 is real and unchanged. link_pairing's diesel::update filters ONLY on asamblea_pairings::id.eq(pairing_id) and estado.eq(EstadoPairing::Pendiente). It has no conjunto_id filter and no expires_at > now() check — exactly as quoted. The reviewer's "presently safe" caveat is accurate. The only caller is get_pairing (handlers.rs:158-179): line 164 calls find_pending_pairings(&mut conn, user.conjunto_id), which IS scoped by conjunto_id, estado=Pendiente, and expires_at.gt(now) (repo.rs:108-111). The pairing.id passed to link_pairing

### LOW-10 — hash_firma uses a fully client-/server-derivable input with no secret — not a tamper-evident signature
- **Where:** `backend/api/src/domains/asamblea/handlers.rs:85-95`  · _security / domains-realtime-asamblea_ · confidence: medium
- **Why:** hash_firma is presented as a vote signature (hash_firma column) but it is a plain SHA-256 over public, predictable fields with no server-held secret/HMAC key. Anyone who knows votacion_id, unidad_id, respuesta and the timestamp can reproduce or forge the value, so it provides no integrity guarantee against an actor with DB write access or an audit reviewer — it is decorative. If this is intended as legal vote-audit evidence (Colombian asamblea law), it gives a false sense of non-repudiation.
- **Fix:** If tamper-evidence is required, HMAC the canonical vote tuple with a server-side secret (or chain hashes into a per-votacion Merkle/append-only log). Otherwise document explicitly that hash_firma is a dedup/idempotency token, not a signature.
- **Verifier:** The quoted code is real and unchanged at backend/api/src/domains/asamblea/handlers.rs:85-95. compute_hash_firma builds input = format!("{votacion_id}:{uid}:{respuesta}:{timestamp}") and runs plain Sha256::digest with NO key/secret/HMAC. A codebase-wide grep for HMAC/hmac/firma found zero keyed hashing anywhere; sha2 plain SHA-256 is the only mechanism. The hash is stored (migrations/2026-06-10-000011_asamblea/up.sql:117 hash_firma text NOT NULL), inserted via cast_voto, and serialized directly into VotoDto (dto.rs:148,162) — but it is NEVER recomputed or verified anywhere. So it is write-only/

### LOW-11 — update_session lets an admin deactivate the assembly via the generic CAS update with no distinct audit/state-machine guard
- **Where:** `backend/api/src/domains/asamblea/handlers.rs:118-137`  · _correctness / domains-realtime-asamblea_ · confidence: low
- **Why:** The CAS itself is correctly implemented (repo.rs:65-89 filters id+conjunto_id+version and bumps version), and that part is solid. However update_session also toggles `activa` through the same path. get_active_session (repo.rs:54-62) does .first() on activa=true with no LIMIT/uniqueness guarantee — there is no DB constraint that at most one asamblea per conjunto is activa. Flipping activa via update_session, or any future create path, can leave multiple active assemblies, and get_active_session would then non-deterministically return one. Minor today but a latent correctness gap around the session state machine.
- **Fix:** Enforce a single active asamblea per conjunto with a partial unique index (conjunto_id) WHERE activa, and route activation/closing through an explicit state-machine endpoint rather than the generic session PUT.
- **Verifier:** The quoted code is real and unchanged. handlers.rs:120 is `let new_activa = req.activa.unwrap_or(current.activa);` and lines 122-131 call repo::update_session passing new_activa and req.version. The reviewer's technical claims hold: (1) The CAS in repo.rs:65-89 correctly filters id + conjunto_id + version and bumps version+1 — solid. (2) get_active_session (repo.rs:54-62) does `.filter(activa.eq(true)).first(conn)` with no LIMIT/ORDER and no uniqueness, so it non-deterministically returns one row if several are active. (3) I confirmed in migrations/.../000011_asamblea/up.sql:5-18 that `asamble

### LOW-12 — Push notifications silently faked in production — LogOnlyPushSender returns Ok and inflates sent count (Law 4 violation)
- **Where:** `backend/api/src/services/push.rs:26-43, 78-80`  · _production / services_ · confidence: high
- **Why:** create_push_sender ALWAYS returns LogOnlyPushSender (it ignores config entirely — the VAPID keys from config.rs vapid_public_key/vapid_private_key are never read). LogOnlyPushSender::send logs and returns Ok(()). In src/domains/citofonia/handlers.rs:209-217 the caller treats every Ok(()) as a delivered push and increments `sent`, returning CallPushResponse { sent } to the client. So a resident triggering a citofonia call gets a response claiming N notifications were dispatched when zero were actually sent. This is exactly the 'NO MOCK FALLBACKS' anti-pattern from Law 4: an unconfigured external dependency (web-push/VAPID) silently fakes a successful result instead of returning 503. The code comments call it a 'Production placeholder', confirming it is the production path, not a test double.
- **Fix:** Implement a real VAPID web-push sender (e.g. the `web-push` crate) constructed only when config.vapid_private_key/vapid_public_key/vapid_subject are all present. When VAPID is unconfigured, either return Arc to a sender whose send() yields an error that the handler maps to 503, or have call_push short-circuit with ApiError::ServiceUnavailable('push no configurado') before claiming any sends. Do not return Ok(()) and count it as `sent`.
- **Verifier:** The quoted code is real and verbatim. push.rs:78-80: `create_push_sender(_config: &Config) -> Arc<dyn PushSender>` always returns `Arc::new(LogOnlyPushSender)` and the underscore-prefixed `_config` means the VAPID keys (config.rs:28-30, 77-79 read VAPID_PUBLIC_KEY/PRIVATE_KEY/SUBJECT from env) are never consumed. push.rs:26-43: `LogOnlyPushSender::send` logs "push notification logged (VAPID not configured)" and returns `Ok(())`. handlers.rs:209-217: the caller does `match state.push_sender.send(...) { Ok(()) => sent += 1, ... }` then returns `CallPushResponse { sent }`. So a resident triggerin

### LOW-13 — WebSocket has no server-side ping/keepalive or idle timeout — dead/half-open connections leak
- **Where:** `backend/api/src/domains/ws.rs:63-76`  · _production / services_ · confidence: medium
- **Why:** The server never sends Ping frames and sets no idle timeout. The recv_task only reacts to inbound frames; a half-open TCP connection (client vanished without TCP FIN/RST — common on mobile/citofonia) is only detected when the send_task next tries to write and fails. For a user who receives no broadcasts (e.g. a quiet conjunto), the send_task blocks in rx.recv() indefinitely and the dead connection — plus both spawned tasks and the broadcast Receiver slot — is never reclaimed. Over time this accumulates ghost connections and tasks. The doc comment claims the recv loop is for 'keep-alive' but nothing actually probes liveness.
- **Fix:** Add a tokio::time::interval that periodically sends Message::Ping and treats a write failure as disconnect, and/or wrap recv with a timeout that closes the socket if no Pong/traffic arrives within a window. This bounds resource usage and detects dead clients promptly.
- **Verifier:** The quoted code at backend/api/src/domains/ws.rs:63-76 is real and unchanged. handle_socket spawns two tasks: send_task loops on rx.recv() over a tokio broadcast::Receiver (ws_hub.rs uses tokio::sync::broadcast, capacity 256), and recv_task only reads inbound frames, breaking on Message::Close. A tokio::select! tears down the connection only when one task completes. I verified there is NO sender.send(Message::Ping(...)) anywhere, no tokio::time::interval, and no tokio::time::timeout — so the server never probes liveness and imposes no idle timeout. With axum 0.8.9 / tokio-tungstenite 0.29, the

### LOW-14 — LiveKit token grants canSubscribe to any tenant member with a valid asamblea token; room scoping relies solely on room name
- **Where:** `backend/api/src/domains/asamblea/handlers.rs:662-711`  · _tenancy / services_ · confidence: low
- **Why:** Tenant isolation for the token itself is correctly enforced — verify_asamblea_tenant gates issuance to the caller's conjunto. The room is scoped by the asamblea UUID, which is globally unique, so cross-tenant room collisions are not possible. This is mostly sound. The minor concern: the LiveKit grant sets room exactly to asamblea-{id} and canSubscribe:true for all authenticated members of the conjunto, with a 6-hour exp (livekit.rs:43). There is no check that the asamblea is currently live/open, so a token remains valid for 6 hours regardless of assembly state, and any resident of the conjunto can join the video room of any asamblea in their own conjunto (no per-asamblea attendee allowlist). Whether that is intended depends on product rules; flagging for confirmation rather than asserting a leak.
- **Fix:** Confirm the intended audience: if only invited/registered attendees should join an asamblea room, add an attendance/eligibility check before issuing the token. Consider shortening exp (6h is long for a live meeting token) and binding issuance to asamblea state (e.g. only when en_curso).
- **Verifier:** All quoted code is real and unchanged. backend/api/src/domains/asamblea/handlers.rs:668 calls repo::verify_asamblea_tenant(&mut conn, id, user.conjunto_id), line 682 builds room_name = format!("asamblea-{id}"), and line 684 sets can_publish = matches!(user.rol, Rol::Administrador | Rol::Concejo) — exactly as quoted. The reviewer cited livekit.rs:43; the file actually lives at backend/api/src/services/livekit.rs, where line 43 is `exp: now + 6 * 3600, // 6 hours` and line 48 hardcodes `can_subscribe: true` for every caller. Tenant isolation IS correctly enforced: verify_asamblea_tenant (repo.rs

### LOW-15 — Push-subscription migration only runs on the first successful user insert, so it is not re-run-safe for transient failures
- **Where:** `backend/migrate/src/steps.rs:651-686`  · _data-integrity / migrate-binary_ · confidence: high
- **Why:** The push_subscriptions INSERT is nested inside the `Ok(1)` (newly-inserted user) arm. On any re-run the parent user insert returns Ok(0) (ON CONFLICT (id) DO NOTHING) and is counted as skipped, so the push-subscription block never executes again. If a push-sub insert failed transiently on the first pass (network blip — the result is discarded with `let _ =`), re-running the migration will never retry it, because the parent user is now an existing row. Idempotency is therefore incomplete for the notifPush → push_subscriptions step.
- **Fix:** Migrate push subscriptions in their own pass keyed off the legacy Usuario.notifPush regardless of whether the user row was just inserted, relying on the endpoint UNIQUE + ON CONFLICT DO NOTHING for idempotency; and stop discarding the insert result with `let _ =` so failures are at least logged.
- **Verifier:** The quoted code is real and unchanged at backend/migrate/src/steps.rs:651-687. The user INSERT at lines 627-650 uses `ON CONFLICT (id) DO NOTHING`. The push_subscriptions INSERT (lines 667-674) is nested entirely inside the `Ok(1)` match arm (newly-inserted user). The `Ok(_) => skipped += 1` arm at line 687 handles the re-run case: an existing user makes the conflict resolve to 0 affected rows, returning `Ok(0)`, so the push-sub block is skipped. The push-sub insert result is discarded with `let _ = target.execute(...)` at line 667, so a transient failure (network blip) is silently swallowed a

### LOW-16 — Legacy plaintext passwords shorter than 6 chars are discarded and forced to a placeholder
- **Where:** `backend/migrate/src/steps.rs:604-617`  · _ux / migrate-binary_ · confidence: medium
- **Why:** Legacy Usuario.password is plaintext in this simulation (prisma/schema.prisma:183, confirmed by spec.md:6-7 'Argon2-hash passwords'), so Argon2-rehashing it is correct by design — this is NOT a double-hash bug. However the `pw.len() >= 6` gate means any user whose existing (valid) password was under 6 chars is silently wiped and forced through must_change_password, even though that password worked in the legacy system. `pw.len()` also counts bytes, so a short multibyte password is judged on byte length. This is a product decision worth surfacing in the migration report rather than applying silently.
- **Fix:** If the intent is a minimum-length policy, record a ReportRow for each user whose password was reset due to length so operators can notify those residents, rather than silently forcing a password change.
- **Verifier:** The quoted code is real and unchanged at backend/migrate/src/steps.rs:605-617. The match arm `Some(ref pw) if pw.len() >= 6 => (hash_password_migration(pw), false)` with the `_` fallback to `hash_password_migration("MIGRATION_PLACEHOLDER_CHANGE_ME"), true)` matches the evidence exactly. Supporting facts all verified: - `str_opt` (steps.rs:65-66) returns None for empty strings (`.filter(|s| !s.is_empty())`), so the `Some(ref pw)` arm only ever sees non-empty passwords; the only gate that can reject a present, non-empty password is the `pw.len() >= 6` check. - `hash_password_migration` (steps.r

### LOW-17 — WebSocket reconnect loop never stops on auth rejection (no backoff, no cap)
- **Where:** `src/components/providers/WebSocketProvider.tsx:52-55`  · _production / frontend-auth-client_ · confidence: high
- **Why:** `onclose` unconditionally schedules a reconnect every 3s with no exponential backoff and no maximum attempt count. If the WS handshake is rejected because the token in the URL is expired/invalid (the common case once the 30-day JWT lapses), the server immediately closes, `onclose` fires, and the client reconnects again 3s later — forever — while `user` is still truthy in the store. This is a permanent 3s-interval reconnect storm against the backend per stale tab, and it never escalates to logging the user out. Because the token is passed as a URL query param (`?token=...`, line 34), it is also exposed in server/proxy access logs and browser history, compounding the localStorage exposure above.
- **Fix:** Add exponential backoff with a max retry cap, and stop reconnecting (and trigger logout) when the close code indicates auth failure (e.g. 1008/4001). Prefer sending the token via a subprotocol or a short-lived ticket rather than a query string so it doesn't land in logs.
- **Verifier:** The cited code is real and exact, though the path prefix is wrong: the file is at /Users/nkt/dev/EN-CONJUNTO/src/components/providers/WebSocketProvider.tsx (frontend), NOT under /backend/. Lines 52-55 match verbatim: ws.onclose unconditionally schedules setTimeout(connect, WS_RECONNECT_INTERVAL) with WS_RECONNECT_INTERVAL = 3000 (line 7). I grepped both WS files (WebSocketProvider.tsx, useWebSocket.ts) for backoff/maxAttempt/retry/Math.pow/Math.min — none exist. So there is genuinely no exponential backoff and no attempt cap: a flat 3s reconnect forever. The auth-rejection mechanism is confir

### LOW-18 — Dead/broken Prisma-era module: seed-utils.ts imports a non-existent './db'
- **Where:** `src/lib/seed-utils.ts:1`  · _maintainability / frontend-auth-client_ · confidence: high
- **Why:** `seed-utils.ts` still uses the pre-migration Prisma client (`db.usuario.findUnique`, `db.unidad.findFirst`, `db.pago.count`, etc.). `src/lib/db` no longer exists (verified: no file/dir matches `src/lib/db*`), so this module cannot compile or run. It is not imported anywhere in `src/` (verified via grep). It only survives the build because `next.config.ts` sets `typescript.ignoreBuildErrors: true`. It's stale auth/seeding logic referencing the old data layer and should be deleted to avoid confusion and accidental revival.
- **Fix:** Delete `src/lib/seed-utils.ts` (and `src/lib/safe-json.ts` if likewise unused) since the Rust backend now owns seeding. If any seeding is still needed client-side, reimplement it against the `api` client.
- **Verifier:** Quoted code is real and unchanged: src/lib/seed-utils.ts:1 is exactly `import db from "./db";`. The module uses Prisma-era delegates throughout: db.usuario.findUnique (lines 8-9), db.unidad.findFirst (lines 19-20), db.pago.count (line 38), and db.pago.create (line 54). The `./db` target does not resolve — there is no src/lib/db.{ts,tsx,js,mjs,cts,mts} and no src/lib/db/index.*; src/lib contains only api/, safe-json.ts, and seed-utils.ts. The module is dead: neither the path "seed-utils" nor the exported symbol autoSeedUserPagos is referenced anywhere else in src/. Prisma is fully gone (no @pri

### LOW-19 — Build ignores TypeScript and ESLint errors, eroding the frontend↔backend contract guarantees
- **Where:** `next.config.ts:16-21`  · _production / frontend-auth-client_ · confidence: medium
- **Why:** The production build will succeed even if `types.ts` DTOs drift out of sync with the Rust structs (the very contract this file is meant to enforce) or if a component calls the API with the wrong shape. The 'tsc clean' ground truth is a manual/CI check, but the deployable artifact does not gate on it — a regression that breaks the auth/DTO contract would still ship. This is why the broken `seed-utils.ts` above goes unnoticed.
- **Fix:** Remove `ignoreBuildErrors`/`ignoreDuringBuilds` (or at minimum run `tsc --noEmit` as a required CI gate before deploy) so contract drift fails the build instead of shipping.
- **Verifier:** The quoted code is real and exact. In /Users/nkt/dev/EN-CONJUNTO/next.config.ts (lines 15-20, the claim's 16-21 is off by one but substantively correct), the config contains: `typescript: { ignoreBuildErrors: true }` and `eslint: { ignoreDuringBuilds: true }`. This means `next build` (the deploy command per package.json "build": "next build", deployed to Cloudflare Pages via wrangler.toml -> .vercel/output/static) will succeed even with type errors or lint errors. I checked for a compensating guard and found none — it actually strengthens the finding: the only CI workflow is /Users/nkt/dev/EN

### LOW-20 — callbackUrl from query string is passed directly to router navigation (minor open-redirect surface)
- **Where:** `src/app/login/page.tsx:64-67`  · _security / frontend-auth-client_ · confidence: low
- **Why:** `callbackUrl` (set by middleware on the redirect, but fully attacker-controllable in a crafted `/login?callbackUrl=...` link) is fed unvalidated into `router.push`/`router.replace`. Next.js's client router generally treats a fully-qualified external URL as a same-origin path, which mostly neutralizes classic open-redirect, but protocol-relative (`//evil.com`) or javascript: style values are worth guarding against defensively. Low confidence on exploitability given Next's router behavior; flagged for hardening.
- **Fix:** Validate that `callbackUrl` starts with a single `/` and is not `//`-prefixed (i.e. a same-origin relative path) before navigating; otherwise fall back to `/inicio`. To confirm exploitability, test `/login?callbackUrl=//evil.com` and `/login?callbackUrl=https://evil.com` against the deployed app.
- **Verifier:** The cited code is real and exact. The file is at /Users/nkt/dev/EN-CONJUNTO/src/app/login/page.tsx (repo root, not under backend/, but the relative path src/app/login/page.tsx:64-67 is correct). Lines 64-67 read: const params = new URLSearchParams(window.location.search); const dest = params.get("callbackUrl") || "/inicio"; setTimeout(() => { router.push(dest); router.refresh(); }, 1000). There is a SECOND identical sink the reviewer also alluded to: lines 28-29 feed the same unvalidated callbackUrl into router.replace() in a useEffect that runs when the user is already logged in. callbackUrl

- INFO — Recipient tenancy is validated on staff paths and self-scoped on resident path (no gap, recorded for completeness) (`backend/api/src/domains/vigilancia/handlers.rs:82-109`)

## What is solid (verified strengths)

- [auth-tenancy] Tenant isolation is consistently and correctly applied across the overwhelming majority of repos: chat, pagos, parqueadero, reservas, comunicaciones, solicitudes, tramites, notificaciones, vigilancia, inmuebles, clasificados all filter every read/write by conjunto_id, and mutations on a path id additionally filter by conjunto_id (e.g. pagos::pagar filters id+conjunto_id+usuario_id, entregar_paquete filters id+conjunto_id, resolver_tramite filters id+conjunto_id+estado). This is a disciplined, greppable pattern.
- [auth-tenancy] The asamblea/ai handlers funnel every assembly sub-resource through verify_asamblea_tenant or get_votacion_with_tenant_check (a join on asambleas.conjunto_id) before touching child tables - a clean, centralized tenant-scoping choke point. The only gap is create_poder (finding #1).
- [auth-tenancy] Login correctly mitigates user-enumeration timing by verifying the supplied password against a cached dummy Argon2 hash when the email is unknown (auth_routes.rs:58-63), and find_by_email is intentionally global since email is the global identity.
- [auth-tenancy] Argon2 uses Argon2id with OWASP-aligned params (m=19456, t=2, p=1) and runs on spawn_blocking off the async executor; verify_password safely returns false for unparseable/empty hashes (password.rs).
- [auth-tenancy] Role guards are explicit, greppable, and unit-tested; require_superadmin correctly excludes plain admins, and superadmin/conjuntos routes all call guard::require_superadmin. The citofonia peer resolver rigorously re-checks caller_conjunto_id on every PeerTarget branch and returns empty (not error) for cross-tenant targets.
- [auth-tenancy] Cookie flags are correct: HttpOnly always set; Secure + SameSite=None only under the cross-site config, else SameSite=Lax. CORS is locked to an explicit allowed-origins list with allow_credentials(true) and no wildcard. Internal error details are redacted from client responses and only logged (error.rs IntoResponse).
- [auth-tenancy] Constitution Law 6 is enforced at config load (rejects :6543 transaction-pooler URLs) and Law 4 is honored for external deps: Gemini and LiveKit return 503 ServiceUnavailable when unconfigured rather than faking results (ai/handlers.rs require_gemini, asamblea livekit_token).
- [domains-operational] Tenant isolation (Law 2) is consistently applied across the read/write paths I reviewed: every query in pagos/repo.rs, reservas/repo.rs, parqueadero/repo.rs, vigilancia/repo.rs, and notificaciones/repo.rs filters by conjunto_id, including the cross-domain create_notificacion helper and the inner-join queries (visitas_de_hoy, paquetes_conjunto, mapa).
- [domains-operational] Cross-tenant recipient injection is explicitly prevented: vigilancia handlers call usuario_en_conjunto(conjunto_id, usuario_id) before creating packages/visits for a client-supplied recipient, and mark_read scopes id-based updates to (conjunto_id, usuario_id) so a user cannot mark another user's notifications read.
- [domains-operational] Money is handled with BigDecimal end-to-end (numeric(14,2) in migrations, BigDecimal in models/DTOs, serialized as String per the Law 6 note) with no float money. recaudo_mes uses diesel::dsl::sum with a clean None->0 fallback.
- [domains-operational] Multi-step writes that must be atomic are wrapped in conn.transaction(...): reservas overlap-check+insert, parqueadero actualizar_celda (state change + audit row), and vigilancia crear_paquete_con_notificacion (package + recipient notification), so partial writes won't occur (the only gap is isolation for the reservation overlap, noted separately).
- [domains-operational] Error handling avoids panics on the hot paths: DB lookups use .optional()? and map to typed ApiError; the few .expect() calls are on always-valid constants (first-of-month, midnight). Internal error detail is redacted to clients (error.rs into_response) while logged server-side, and unique violations are mapped to 409 Conflict.
- [domains-operational] Role gating is explicit and greppable via guard::require with per-handler role constant lists (ROLES_PARQUEADERO, ROLES_VIGILANCIA, ROLES_RONDAS), and VIGILANTE is correctly narrowed to own rows in registros via solo_de_usuario.
- [domains-content] Tenant isolation (Law 2) is consistently applied across the content domains: every list/find/insert/update/delete in comunicaciones, solicitudes, tramites, clasificados and inmuebles repos filters or sets conjunto_id (e.g. comunicaciones/repo.rs eliminar_anuncio filters both id AND conjunto_id; tramites find_tramite and resolver_tramite both scope by conjunto_id; notificaciones helpers all carry conjunto_id).
- [domains-content] The trámite resolver side-effect is genuinely correct and transactional: resolver_tramite wraps the state transition, the vehiculo/mascota insert, and the requester notification in one conn.transaction, and the UPDATE filters on estado = Pendiente so a concurrent resolver loses the race and gets 409 — a duplicate placa aborts the whole transaction and the trámite stays PENDIENTE, exactly as the docstring claims.
- [domains-content] Role gating is explicit and greppable via guard::require: anuncio create/delete require ADMINISTRADOR|CONCEJO, directorio requires admin/concejo/gate staff, and trámite resolution requires ADMINISTRADOR only. guard.rs has unit tests asserting Forbidden for unlisted roles.
- [domains-content] Resident-vs-admin list scoping is handled correctly: listar_solicitudes and listar_tramites compute solo_usuario = Some(user.id) for non-admin roles and None for ADMINISTRADOR/CONCEJO, so residents only see their own PQRS/trámites while admins see the whole conjunto.
- [domains-content] The directorio endpoint deliberately limits returned PII to (id, nombre, torre, apto, telefono) with a Habeas-Data comment, omitting email/avatar — a thoughtful privacy choice for the resident directory.
- [domains-content] JSON-array/text columns (archivos_url, imagenes, documentos, caracteristicas) are validated at the boundary with serde_json::to_value and gracefully decoded with unwrap_or_default on read, and money fields use BigDecimal serialized as string per Law 6.
- [domains-content] Trámite payloads are strongly typed only at approval time with deny_unknown_fields on VehiculoPayload/MascotaPayload, returning 422 Unprocessable on a shape mismatch rather than silently inserting garbage — a clean validation boundary.
- [domains-content] DB error mapping is correct: diesel UniqueViolation is translated to ApiError::Conflict (409) in error.rs, so the resolver's duplicate-placa 409 contract is backed by real plumbing (the only issue is the constraint's scope, noted as a finding).
- [domains-realtime-asamblea] CAS optimistic lock on the assembly session is correctly implemented: update_session (asamblea/repo.rs:65-89) filters by id + conjunto_id + expected version and increments version atomically, and the handler returns 409 on rows==0 — genuinely prevents lost updates.
- [domains-realtime-asamblea] Tenant isolation is consistently enforced across the read/write paths reviewed: verify_asamblea_tenant, get_votacion_with_tenant_check (via inner_join to asambleas + conjunto filter), chat repo functions, citofonia resolve_targets, and admin_stats all filter by conjunto_id from the JWT (user.conjunto_id), never from client input — matching Law 2.
- [domains-realtime-asamblea] Pairing PINs are Argon2id-hashed at rest (create_pairing -> hash_password_blocking) with OWASP params (m=19456,t=2,p=1); the migration comment confirms legacy plaintext login passwords were dropped. Verification runs off-executor via spawn_blocking. This satisfies Law 3 for PIN storage.
- [domains-realtime-asamblea] Role guards are explicit and greppable at the handler boundary: superadmin conjuntos routes use require_superadmin, admin_stats uses require_admin, chat admin endpoints and asamblea mutating session/votacion/turno/poder-verify endpoints call guard::require(ADMIN_ROLES). guard.rs has unit tests asserting the superadmin gate excludes plain admins.
- [domains-realtime-asamblea] admin_send and get_thread in chat correctly validate the target resident belongs to the caller's conjunto (exists check / get_resident_info filtered by conjunto_id) before sending or exposing thread + PII — no cross-tenant chat leakage.
- [domains-realtime-asamblea] LiveKit token endpoint honors Law 4: returns 503 ServiceUnavailable when api_key/secret are unconfigured instead of faking a token; publish capability is correctly restricted to Administrador/Concejo.
- [domains-realtime-asamblea] citofonia parse_peer_id is well-tested and resolve_targets re-checks the embedded conjunto_id against the caller's conjunto, returning empty (no push) on mismatch — preventing cross-tenant call-push.
- [domains-realtime-asamblea] Single-active invariants for votaciones (update_votacion deactivates siblings in a transaction) and turnos (update_turno completes prior HABLANDO in a transaction) are implemented with proper transactional scoping.
- [services] Per-tenant WebSocket isolation is correct: ws_hub keys broadcast channels by conjunto_id (HashMap<Uuid, Sender>), and ws.rs derives conjunto_id from the verified JWT claims (not client input), so a client can only ever subscribe to its own tenant's channel.
- [services] WebSocket authentication is enforced before upgrade: jwt::verify is checked and returns 401 on failure (ws.rs:27-30) before on_upgrade, so unauthenticated sockets are rejected.
- [services] target_user_id fan-out filtering is implemented for user-specific events (chat) — ws.rs:48-52 skips events whose target_user_id != this socket's user_id, and chat/handlers.rs sets target_user_id appropriately for direct admin replies.
- [services] Law 4 is correctly honored for Gemini: require_gemini() returns ApiError::ServiceUnavailable -> HTTP 503 when GEMINI_API_KEY is absent (ai/handlers.rs:39-44, error.rs:38), and upstream Gemini failures map to 502 BAD_GATEWAY rather than being faked.
- [services] Law 4 is correctly honored for LiveKit: livekit_token returns ServiceUnavailable (503) when api_key/secret are unconfigured (asamblea/handlers.rs:675-679) instead of issuing a bogus token.
- [services] Gemini HTTP client sets an explicit 30s timeout (gemini.rs:59-61) and checks resp.status().is_success() before parsing, bailing with status+body on error — solid external-HTTP hygiene.
- [services] Citofonia push targeting is tenant-isolated and injection-safe: every resolve_targets branch filters usuarios/unidades by caller_conjunto_id and rejects cross-conjunto PeerTarget values, and the push_subscriptions query is double-filtered by conjunto_id AND usuario_id IN (resolved ids).
- [services] Chat audio upload paths are server-generated (chat-voice/{conjunto_id}/{uuid}.webm) with no user-controlled path segments, so the S3 key cannot be steered for path traversal; conjunto_id also namespaces objects per tenant.
- [services] S3Storage checks the response status_code() >= 300 and bails with the body on failure rather than assuming success (storage.rs:68-74).
- [backend-infra] Law 6 (DB connection) is enforced at startup: config.rs:103-108 `validate_not_transaction_pooler` rejects any DATABASE_URL containing :6543 with a clear, constitution-citing error, and it is unit-tested (rejects 6543, accepts 5432/direct).
- [backend-infra] Error handling does not leak internals: error.rs:72-78 logs the full `anyhow` chain via tracing but returns a generic 'internal error' detail to clients, and responses are proper RFC-7807 `application/problem+json`. There is an explicit `internal_detail_is_redacted` test.
- [backend-infra] Diesel error mapping is sensible: NotFound -> 404, UniqueViolation -> 409 Conflict, everything else -> 500 Internal (error.rs:96-107), keeping DB internals out of client responses.
- [backend-infra] TLS is handled correctly without libpq: db/mod.rs uses rustls + webpki-roots for all Postgres connections (including migrations), with an idempotent crypto-provider install, matching a slim container image.
- [backend-infra] Migrations are gated behind RUN_MIGRATIONS and can use a separate direct connection (MIGRATIONS_DATABASE_URL) per Law 6, run via spawn_blocking so the async runtime is not blocked (db/mod.rs:57-77, main.rs:15-21).
- [backend-infra] Request body is capped at 2 MiB via RequestBodyLimitLayer (lib.rs:22,55), a sane bound for the base64 document payloads described in the comment.
- [backend-infra] Config secrets are required explicitly: DATABASE_URL and JWT_SECRET use `require()` which rejects empty values, and most optional service vars are `.filter(|v| !v.is_empty())` so blank env vars don't masquerade as configured.
- [backend-infra] Structured logging is production-ready: main.rs:33-42 supports JSON log format via LOG_FORMAT=json and an EnvFilter defaulting to 'info'.
- [backend-infra] healthz performs a real DB liveness probe with a 3s timeout (db/mod.rs:80-88) and degrades to db:'error' rather than failing the whole endpoint.
- [migrate-binary] Deterministic id mapping is correct and well-tested: cuid_to_uuid uses a fixed UUIDv5 namespace (idmap.rs:4-11) so re-runs produce identical ids, and idmap.rs has unit tests for determinism and null/empty handling (deterministic, nulls).
- [migrate-binary] Argon2 rehash parameters in the migrate crate (m=19456, t=2, p=1, Argon2id, V0x13 — steps.rs:2483-2493 and seed.rs:57-64) match the API's password module, and rehashing legacy *plaintext* passwords is the correct behavior here (not a double-hash), with empty/garbage passwords mapped to an unusable placeholder + must_change_password as the spec requires.
- [migrate-binary] JSON repair is safe and auditable: parse_json_or_null (steps.rs:24-44) never panics on malformed legacy JSON, substitutes NULL, and records a ReportRow with the table/legacy_id/column/truncated-raw-value/action so every repaired column is traceable in the CSV report.