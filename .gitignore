# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# Android/Play signing material. THIS REPO IS PUBLIC — a keystore or its
# passwords committed here is published to the internet permanently (git history
# + GitHub's fork network survive a later delete) and lets anyone sign releases
# as this app. Never opt these back in.
*.jks
*.keystore
keystore-credentials.txt
*.credentials.json

# vercel
.vercel

# cloudflare wrangler (local state / build cache — never commit)
.wrangler/

# stray build logs
/build_log.txt
/nop_log.txt

# this project uses pnpm; npm's lockfile must not be committed
package-lock.json

# throwaway dev scratch / tmp scripts (never commit — past leaks lived here)
/scratch/
/tmp/

# rust build output
target/

# agent/tooling state (claude-flow, ruflo, swarm) — not source
.claude-flow/
.swarm/
**/.claude-flow/

# typescript
*.tsbuildinfo
next-env.d.ts

# landing (export estático)
/landing/.next/
/landing/out/
